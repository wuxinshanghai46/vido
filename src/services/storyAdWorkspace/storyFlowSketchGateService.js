'use strict';

// Compatibility module name retained for internal imports. The old paid image
// flow is disabled; the only authority is now the zero-cost structured contract.
module.exports = require('./storyFlowContractService');
