# VIDO 剧情广告 2026-07-23 公司继续验证交接

> **最新完整交接请读取：** `docs/handoffs/2026-07-23-home-to-office-complete.md`。该文档包含今晚 V2.0、付费门禁、全中文门禁、年龄修复、十五秒新任务和场景计费状态不明确的最终状态；本文仅保留凌晨历史记录，不再作为明日操作基线。

> 交接时间：2026-07-23 00:30（Asia/Shanghai）
> 目标分支：`codex/story-ad-v3-upgrade`
> 本文档生成前最终业务提交：`521d5e253720ef869c49a6ac3f1edb1649da236e`
> 本文档生成前代码树：`ed05c6abd17be494fc94450efa35695f156ace8b`
> 目标生产任务：`fd30ac4c-d54b-44c2-bab7-268fc622b5e5`

## 一、当前结论

1. 今晚业务代码已经全部提交并推送到 `origin/codex/story-ad-v3-upgrade`，并同步到生产服务器。
2. 本文档提交前，本地、远程 Git、生产服务器业务提交均为 `521d5e2`，代码树均为 `ed05c6a`；生产工作树干净。
3. 生产 PM2 `vido` 为 `online`，生产内网、生产外网及本地健康检查均为 HTTP 200。
4. 目标任务没有活动生成任务，最终成片尚未生成；今晚最后的状态核对均为只读，没有触发图片、视频、QA、TTS 或封装调用，也没有产生新费用。
5. 第 2 镜当前镜头合同已经修改，但匹配新合同的分镜图尚未重新生成，因此视频预检会被 `VIDEO_INPUT_QA_REQUIRED` 阻断。
6. 当前 `video_clips` 聚合输出和 `final_video` 均为空，页面会把 6 镜都投影成缺少当前视频素材。**这不等于 6 镜都需要重新付费生成。**
7. 服务器磁盘上第 1、3、4、5、6 镜的历史成功视频文件均真实存在，可以继续调查并安全恢复引用；第 2 镜没有视频文件。
8. 明天不能直接全选 6 个付费单元。先重新生成第 2 镜分镜图并通过图片 QA，再做只读预检；如果仍显示 6 个付费单元，必须停止并先恢复现有视频引用。

## 二、今晚完成的主要更新

### 1. 页面结果只显示当前真实状态

- 区分当前生成成功、真实失败、前置校验失败未执行、保留旧片段和最终封装被阻断。
- 当前尝试失败时，不再同时显示“全部镜头已通过”。
- 已完成的单元会明确说明是否保留、是否会再次收费；失败单元会显示支持编号、镜头范围和真实原因。
- 历史成功尝试不再被错误投影成当前失败，也不会掩盖当前失败。

### 2. 新视频流程成为唯一有效流程

- 视频预检、提交、QA、恢复和最终封装统一校验当前合同、生成批次及素材来源。
- 旧流程产物不能再因为存在文件就被静默当成当前有效结果。
- 语义合同身份与供应商生产批次身份分离，避免审核时间等非语义字段造成错误失效，同时禁止真实输入变化后继续复用旧产物。
- 最终封装前会再次核对逐镜来源、逐镜 QA 和相邻镜头边界证据。

### 3. 费用确认与默认选择

- 两处范围/费用确认复选框默认勾选，减少用户不知道还需确认的困惑。
- 付费镜头仍默认不选择，不能因为确认框默认勾选就自动提交费用。
- 自动付费重试固定为 0；内容或选择范围变化后必须重新预检和确认。

### 4. 跨镜头 QA 与成功片段保护

- 边界 QA 同时检查相邻两侧，不再只检查单边。
- 成功生成的视频即使后续边界 QA 未通过，也会保留文件、供应商任务号和计费证据。
- 边界 QA 在最终素材来源身份确定后绑定，避免先审后换素材导致证据错配。

### 5. 真人隐私拒绝的真实处理

- 第 2 镜当前失败码为 `INPUT_PERSON_PRIVACY`，发生在供应商提交之前。
- 该次没有供应商视频任务号，计费状态为 `not_submitted`，费用为 0。
- 同一合同、同一路径不能反复付费重试；必须先修改画面合同并重新生成匹配的分镜图。
- 该规则只阻断命中隐私风险的具体生成单元，不会因为其他场景也有人物就全局禁止真人镜头。

### 6. 阻断范围收敛到具体单元

- 全局条件不满足时才阻断全部付费单元。
- 某一镜头自身不满足条件时，只禁用该镜头，并在镜头行内显示不可选择的具体原因。
- 不再因为第 2 镜输入隐私失败而连带禁用其他可安全处理的镜头。
- 浏览器静态资源版本已更新为 `20260722-unit-block-scope-v3`。

## 三、今晚关键提交

| 提交 | 内容 |
|---|---|
| `7256c12` | 明确部分成功、当前失败和未执行的页面结果 |
| `8605b1a` | 强制使用当前视频素材及来源流程 |
| `87107ba` | 费用确认前置检查和默认确认状态 |
| `83c48e4` | 边界 QA 失败时保留成功视频片段 |
| `6df8c6a` | 在最终来源身份确定后绑定边界 QA |
| `f169980` | 统一当前媒体失败状态和页面展示 |
| `a9d6216` | 历史成功尝试不再污染当前结果 |
| `521d5e2` | 将生成阻断范围收敛到具体单元 |

`2821954` 为上一份交接文档提交，不属于业务逻辑修改。

## 四、目标任务当前真实状态

### 4.1 已确认事实

| 项目 | 当前值 |
|---|---|
| 任务状态 | `done` |
| 当前阶段 | `keyframe_contract_ready` |
| 任务更新时间 | `2026-07-23 00:23:00`（Asia/Shanghai） |
| 活动生成 | 无，`active_generation_id` 为空 |
| 模型调用数 | 234；最后多次只读核对未变化 |
| 关键帧 | 6 镜均有图片记录且图片 QA 通过 |
| 第 2 镜合同 | 已修改 |
| 第 2 镜匹配新合同的分镜图 | 尚未生成；当前图与新合同不一致 |
| 当前视频聚合输出 | `video_clips = null` |
| 最终成片 | `final_video = null` |
| 当前视频预检 | `blocked / VIDEO_INPUT_QA_REQUIRED` |
| 当前阻断原因 | 第 2 镜画面与当前镜头合同不一致，需要重新生成分镜图 |

注意：这里的 `task.status = done` 只表示本次镜头合同编辑阶段已经完成，**不表示视频已生成完成，也不表示最终成片完成**。

### 4.2 历史视频文件核对

| 镜头 | 历史状态 | 服务器文件 | 当前说明 |
|---|---|---|---|
| 第 1 镜 | 历史生成成功 | 存在，约 1.00 MB | 当前未被 `video_clips` 聚合输出引用 |
| 第 2 镜 | `INPUT_PERSON_PRIVACY` | 不存在 | 未提交供应商、未计费；需先重新生成匹配新合同的分镜图 |
| 第 3 镜 | 历史生成成功 | 存在，约 1.49 MB | 当前未被 `video_clips` 聚合输出引用 |
| 第 4 镜 | 历史生成成功 | 存在，约 1.01 MB | 计费证据保留；当前未被聚合输出引用 |
| 第 5 镜 | 历史生成成功 | 存在，约 0.39 MB | 计费证据保留；当前未被聚合输出引用 |
| 第 6 镜 | 本地运动方案成功 | 存在，约 0.25 MB | 当前未被聚合输出引用 |

### 4.3 已确认事实、代码根因与待确认推断

- **已确认事实：** 第 2 镜合同与现有分镜图不一致；`video_clips` 当前为空；5 个历史成功视频文件仍存在；页面因此会计算出 6 个缺少当前视频的付费候选。
- **已确认代码行为：** 当前素材兼容投影只认可当前 `video_clips` 中满足合同和来源要求的片段；逐镜历史状态不会自动恢复成当前聚合结果。
- **尚未闭环的推断：** 第 2 镜合同编辑可能触发了下游视频聚合输出失效或删除，但明天必须继续追踪完整写入/失效数据流后才能作为根因结论。
- **当前安全策略：** 不降低来源或 QA 标准，不直接把历史文件强行标记为通过；先验证每个历史文件与当前合同、生成身份和 QA 证据是否兼容，再恢复可复用引用。

## 五、明天到公司后的 Git 拉取步骤

在公司电脑项目目录执行：

```powershell
git status --short
git fetch origin
git switch codex/story-ad-v3-upgrade
git pull --ff-only origin codex/story-ad-v3-upgrade
git log -5 --oneline
npm run story-ad:v3:test
node src/server.js
```

然后访问：`http://localhost:3007`

如果 `git status --short` 显示有未提交内容：

1. 先确认这些内容属于谁、是否需要保留；
2. 需要保留时先提交或暂存，再执行 `git pull --ff-only`；
3. 不要使用 `git reset --hard` 覆盖本地文件。

## 六、明天页面操作顺序

### 第一阶段：只处理第 2 镜分镜图

1. 按 `Ctrl+F5` 强制刷新，确认加载最新静态资源。
2. 打开目标任务并回到第 4 步“分镜生成”。
3. 核对第 2 镜当前描述确实是修改后的远景/全景等安全构图，不再是会命中隐私校验的直接真人首帧路径。
4. **只点击第 2 镜的“重新生成分镜图”。** 这里重新生成的是第 2 镜图片，不是视频，也不是 6 镜全部图片。
5. 等待第 2 镜新图片生成并通过图片 QA；不要同时重做其他已通过分镜图。

### 第二阶段：只读视频预检

1. 回到第 5 步“广告合成”，打开视频生成/重做单元弹窗。
2. 先查看候选数量、每镜可选状态、费用和不可选原因，不要立即提交。
3. 如果页面仍显示 6 个付费单元或要求整条重做，立即取消弹窗。
4. 继续排查 `视频聚合输出失效 → 逐镜历史状态 → 文件存在性 → 当前合同/来源兼容 → QA 证据 → video_clips 恢复` 的完整数据流。
5. 只有确认第 1、3、4、5、6 镜哪些可以安全恢复、页面费用范围不再错误扩大后，才重新讨论第 2 镜视频生成。

### 第三阶段：满足门禁后再决定是否付费

只有同时满足以下条件，才能进入费用确认：

- 第 2 镜新分镜图已经通过图片 QA；
- 第 1、3、4、5、6 镜现有片段已完成来源、合同、文件和 QA 兼容核对；
- 页面只列出真正需要生成的镜头，不再错误显示 6 个付费单元；
- 自动重试为 0；
- 预计费用和最高费用与实际选择镜头一致；
- 没有活动生成任务，也没有会覆盖已有视频的并发操作。

## 七、遇到以下情况立即停止

- 第 2 镜尚未重新生成匹配新合同的分镜图，就允许提交视频；
- 页面显示 6 个付费单元或默认全选；
- 页面把“任务 done”解释为最终视频已完成；
- 第 1、3、4、5、6 镜明明有服务器文件，却仍要求无解释地全部付费重做；
- 自动重试不是 0；
- 费用确认范围与实际选择镜头不一致；
- 未完成二次费用确认就出现新的供应商任务号；
- 页面同时出现“生成失败”和“全部已通过”；
- 任何修复方案试图删除来源门禁、降低 QA 标准或直接伪造当前通过状态。

## 八、今晚真实执行的验收

- 本地完整 `npm run story-ad:v3:test`：通过；过程中发现并修正两处架构门禁后重新完整执行通过。
- 生产隔离测试和生产工作目录完整 `npm run story-ad:v3:test`：通过。
- 定向回归覆盖：当前/历史结果展示、素材来源硬门禁、费用确认、边界 QA、成功片段保留、失败状态统一、历史尝试隔离和逐单元阻断。
- JavaScript 语法、架构门禁及 `git diff --check`：通过。
- 生产部署备份：`/opt/vido/backups/story-ad-unit-block-scope-20260723001340`。
- 生产 PM2 `vido`：`online`。
- 本地、生产内网、生产外网健康检查：HTTP 200。
- 本文档生成前，本地、远程 Git、生产服务器提交和代码树完全一致。
- 目标任务最终只读核对：无活动生成；模型调用数 234；任务更新时间未变化。
- 逐镜历史文件只读检查：第 1、3、4、5、6 镜文件存在，第 2 镜无文件且未提交供应商。
- 今晚代码验证、部署和状态核对均未触发真实视频、图片、TTS 或最终封装，不产生新的媒体费用。

## 九、明天继续开发的优先级

1. 先完成第 2 镜新合同对应的分镜图生成与图片 QA。
2. 追踪第 2 镜合同编辑后 `video_clips` 为空的最早错误源，确认是主动失效、删除、覆盖还是历史迁移缺失。
3. 为“编辑一镜后保留其他兼容视频片段”新增稳定复现测试，并覆盖历史数据、文件缺失、并发和失败恢复。
4. 在不降低来源与 QA 门禁的前提下，恢复第 1、3、4、5、6 镜可复用的当前引用和真实计费证据。
5. 重新执行定向回归、完整回归、静态检查；生产部署后再次核对任务、文件、PM2 和健康状态。
6. 只读预检确认付费范围准确后，再由用户决定是否生成第 2 镜视频。
7. 全部镜头真实通过后才进行配音、BGM、字幕和最终封装。

## 十、本地未纳入交接提交的用户文件

本地仍保留 5 个用户原有未跟踪文件，包括：

- `docs/plans/new-story-ad-commercial-hardening-plan-2026-07-13.md`；
- `docs/reports/new-story-ad-comprehensive-audit-2026-07-12.md`；
- 3 个名称异常或历史导出的 HTML/Markdown 文件。

这些文件均未修改、未删除、未暂存，不会纳入本次交接提交。后续由用户单独决定保留、改名、提交或删除。
