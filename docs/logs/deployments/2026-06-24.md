# VIDO 部署日志 - 2026-06-24

> 自动生成 · 项目助理维护 · 一天一文件

## 当日概览
AI 小说章节目录对齐正文文本框的前端修正已部署到生产。

## 事件流水

## [00:01] 部署
- 提交 `6e40cdd fix: align novel chapter list to text area` 已同步到生产 `/opt/vido/app`，远端同步前已备份 AI 小说 HTML/CSS/JS。
- 本次为 AI 小说前端静态资源变更，未重启 PM2。
- 验证：公网 `/api/health` 返回 200；公网 AI 小说 HTML 使用 `20260623-chapter-list-text-align`，JS 包含按正文框底边计算章节目录高度的逻辑，CSS 保留章节目录内部滚动。

## [00:44] 部署
- 提交 `469093c fix: count punctuation in novel word totals` 已同步到生产 `/opt/vido/app`，远端同步前已备份 AI 小说 HTML/JS、小说路由和数据库模型文件。
- 本次包含后端统计口径变更，已执行 PM2 reload；短暂重载窗口后服务恢复 online。
- 验证：公网 `/api/health` 返回 200；公网 AI 小说 HTML 使用 `20260624-count-punctuation`，JS 字数函数只排除空白，线上 Node 样例确认标点计入统计。

## [01:11] 部署
- 提交 `a941dbc feat: delete unsubmitted novel chapters` 已通过 bundle 快进同步到生产 `/opt/vido/app`，部署前备份 AI 小说 HTML/CSS/JS 和小说路由文件。
- 本次包含后端路由变更，已执行 PM2 reload。
- 验证：本地 `node --check`、数据库 schema 校验、生产 `node --check` 均通过；公网 `/api/health` 返回 200；公网 AI 小说 HTML/JS/CSS 命中 `20260624-delete-draft-chapter` 版本和删除章节逻辑标记。
