# VIDO 部署日志 - 2026-06-23

## 事件流水


## [00:26] 部署
- 上传 AI 小说前端文件到生产 /opt/vido/app，并完成远端备份。
- 验证：远端 JS 语法通过，公网 /ai-novel.html 返回 200，版本 20260623-split-autofill 生效。

## [00:39] 生产同步校验
- 补传 package.json、package-lock.json、src/services/novelService.js 到生产并执行 npm install --omit=dev。
- PM2 vido reload 后健康检查 200；公网 AI 小说页面版本 20260623-split-autofill 生效。
- 通过 SHA256 确认本次 7 个文件在本地与生产内容一致。

## [20:49] 部署
- 以服务器为最终版完成 Git 与本地同步；随后通过 Git bundle 将生产服务器仓库 HEAD 对齐到 origin/master。
- 最终提交: 4862455。生产 PM2 应用健康接口返回 200。
- 未执行 git clean，保留生产服务器未跟踪历史备份/运行文件；未记录任何凭证。

## [21:09] 部署
- 已将 AI 小说删除、最近作品直达、字数统计和章节编辑布局修复部署到生产。
- 生产已物理清理 1 条历史 deleted 小说记录；PM2 vido 重启后 /api/health 返回 200。
- 验证: node --check、npm run db:verify-schema、本地健康检查、生产健康检查、生产静态资源特征检查均通过。未记录任何凭证。
## [22:07] 部署
- 提交 `a38b159 fix: preserve novel edits and clarify ad controls` 已推送到远端 master。
- 线上 `/opt/vido/app` 已同步到该提交并重启 PM2 `vido`。
- 验证：公网 `/api/health` 返回 ok；线上静态资源包含小说保存标记、剧情广告制作控制说明与样式；写作服务包含 story-first prompt 约束。

## [22:19] 部署
- 提交 `3ee7514 fix: update novel word counts while editing` 已推送到远端 master。
- 线上 `/opt/vido/app` 已同步到该提交；本次仅前端静态文件变更，未重启 PM2。
- 验证：公网 `/api/health` 返回 ok；公网 `/ai-novel.html` 包含 `20260623-live-autosave`；公网 `/js/ai-novel.js` 包含即时字数刷新和 350ms 自动保存逻辑。

## [22:38] 部署
- 提交 `fcbe998 fix: preserve active page on refresh` 已推送到远端 master。
- 线上 `/opt/vido/app` 已同步到该提交；本次为前端路由和 HTML 版本号变更，未重启 PM2。
- 验证：公网 `/api/health` 返回 ok；公网 JS 静态资源包含主工作台、管理后台、AI 小说的路由恢复逻辑。

## [22:52] 部署
- 提交 `ec92bd6 fix: speed up route restore on refresh` 已同步到生产 `/opt/vido/app`。
- 本次仅前端静态文件和 HTML 版本号变更，未重启 PM2。
- 验证：公网 `/api/health` 返回 200；公网主工作台脚本不再包含延迟切回逻辑，后台脚本不再包含延迟 tab 点击，AI 小说脚本包含路由优先加载作品逻辑。

## [23:08] 部署
- 提交 `7ff9905 fix: preserve routes across production modules` 已同步到生产 `/opt/vido/app`。
- 本次扩展数字人、漫剧工作台和剧情工作台刷新恢复；仅前端静态文件变更，未重启 PM2。
- 验证：公网 `/api/health` 返回 200；公网数字人和剧情工作台脚本包含新增路由状态；远端漫剧 HTML 包含 `MANGA_ROUTE_KEY`、project、episode、shot 状态写入逻辑。

## [23:17] 部署
- 提交 `502af94 fix: move novel chapter actions to top` 已同步到生产 `/opt/vido/app`。
- 本次仅 AI 小说前端静态文件变更，未重启 PM2。
- 验证：公网 `/api/health` 返回 200；公网 AI 小说 JS 包含 `nv-chapter-list-actions` 且不再包含旧底部内联样式；公网 CSS 包含顶部工具条和列表独立滚动样式。

## [23:30] 部署
- 提交 `b84bff0 fix: keep new novel chapters in write view` 已同步到生产 `/opt/vido/app`，远端同步前已备份 AI 小说相关静态文件。
- 部署后 PM2 已 reload，进程 online；短暂重启窗口后健康检查恢复正常。
- 验证：公网 `/api/health` 返回 200；公网 AI 小说 HTML 使用 `20260623-add-chapter-stay`，JS 包含保持当前面板与新增章节滚动逻辑，CSS 包含章节新增高亮样式。

## [23:40] 部署
- 提交 `edc79d5 fix: cap novel chapter list height` 已同步到生产 `/opt/vido/app`，远端同步前已备份 AI 小说 HTML/CSS。
- 本次仅前端静态样式和 HTML 版本号变更，未重启 PM2。
- 验证：公网 `/api/health` 返回 200；公网 AI 小说 HTML 使用 `20260623-chapter-list-height`，CSS 包含章节目录固定高度、sticky 和移动端 70vh 限制。

## [23:46] 部署
- 提交 `b63d4f1 fix: align novel chapter list height` 已同步到生产 `/opt/vido/app`，远端同步前已备份 AI 小说 HTML/CSS。
- 本次仅前端静态样式和 HTML 版本号变更，未重启 PM2。
- 验证：公网 `/api/health` 返回 200；公网 AI 小说 HTML 使用 `20260623-chapter-list-align`，CSS 包含新的章节目录高度公式和 sticky/移动端限制。

## [23:53] 部署
- 提交 `e2ea4ce fix: stretch novel chapter list to grid row` 已同步到生产 `/opt/vido/app`，远端同步前已备份 AI 小说 HTML/CSS。
- 本次仅前端静态样式和 HTML 版本号变更，未重启 PM2。
- 验证：公网 `/api/health` 返回 200；公网 AI 小说 HTML 使用 `20260623-chapter-list-stretch`，CSS 包含 `align-self: stretch`、`height: auto`、目录容器隐藏溢出和内部列表滚动。
