# VIDO 变更日志 - 2026-08-07

> 项目助理维护 · 只记录代码、配置和交接文档变更

## [00:25] 新增：v71 公司续接交接
- `docs/handoffs/2026-08-07-v71-company-continuation-handoff.md`：记录 v65/v67/v69/v71 更新、数据流、三方一致性、验证证据、风险边界和公司续接步骤。
- 本次不涉及 UI/UX、业务代码、配置或生产数据修改；5 个既有未跟踪文档保持原样。

## [22:02] 新增与修复：平台 Skill AI 创作链 v78
- `src/services/newStoryAd/contentSkillService.js`、`contextBuilder.js`：新增广告/纯剧情服务端 Skill 定义与任务快照，生成提示显式隔离两类内容。
- `storyAdService.js`、`assistKnowledgePolicyService.js`、`sceneExperienceAssistService.js`：人物、场景和 360/3D 规划辅助使用任务知识策略快照；新增结构化空间规划输出合同。
- `assetCenterAssist.js`、`assetCenterView.js`、`assetCenterPlanningDetails.js`、`sceneWorldView.js`：接入人物/场景 AI 帮写、AI 完善空间规划和结构化 3D 导演预演，并明确真实 6DoF 供应商边界。
- `scenePanoramaService.js`、`newStoryAd.js`：360 以服务端最新指纹作为唯一付费计划授权，保留过期计划拒绝并补上任务所有权校验。
- `storyboardSketchService.js`：线稿提示加入故事蓝图、场景用途、前后镜状态、银幕方向、轴线、视线、道具状态和知识策略指纹。
- `taskDeletionService.js`、`storageService.js`、`app.js`、`projectStore.js`：新增永久删除按钮、平台危险确认弹窗、运行任务取消、记录清理、专属文件删除、共享文件保留与目录越界防护。
- `styles.css`：目标设置控件统一 42px 高度和网格对齐；项目操作列容纳打开/删除。
- `config/story-ad-release.json`：发布版本更新为 `20260807-platform-skills-ai-v78`，合同 `platform-skills-v1`；发布清单和运行清单已重建。
- 新增/扩展 3 组回归，V6 全套、V3 全链路、发布完整性和架构体积门禁全部通过，真实模型调用 0。

## [23:39] 修复：人物多场景造型合同 v81
- 新增 `personLookProfileService.js`、`personLookProjectionService.js` 和 `assetCenterPersonLooks.js`，以稳定 `look_id`、场景绑定和主造型兼容投影替代单一服装标量。
- `contextBuilder.js`、`assetPlanService.js`、`assistSubjectProfileService.js`、`generationSpecCompletionService.js`、`subjectAssetBundleService.js`、`referenceSelectionService.js`、`keyframeContractService.js`、`storyboardTableService.js` 等全链路改为按造型处理，规划器不得折叠已确认的多造型。
- 人物资产编辑器新增独立造型卡、增删和 AI 辅写；生成确认披露造型数量/调用数量，未绑定场景的多造型在付费请求前阻断。
- 新增 `repair-new-story-ad-person-looks.js` 精确迁移工具及多造型、迁移回归；发布版本更新为 `20260807-person-multi-look-v81`、合同 `person-multi-look-v1`。
- 定向回归、V3/V6 结构门禁、平台全量升级回归、发布完整性/原子性/闭包检查和 `git diff --check` 均通过；真实模型调用 0。
