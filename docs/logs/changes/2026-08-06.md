# VIDO 代码变更日志 - 2026-08-06

> 自动生成 · 项目助理维护 · 一天一文件

## [00:18] Bug：再次未知计费未持续锁定、场景补齐器不可达
- `assetCenterBillingRetry.js`：同时读取整体和主体分支 `billing_state=unknown`，即使顶层错误码为供应商 5xx，也继续显示费用风险锁定入口。
- `sceneBindingService.js`、`sceneAssetService.js`：常规解析仍严格拒绝不完整合同；只有生成服务明确标记自动补齐流程时才允许进入补齐器，补齐后仍通过完整场景合同门禁再调用图片。
- `test-new-story-ad-generation-spec-completion.js`、视觉同步测试：新增严格默认、显式补齐入口、再次 5xx 锁定回归；发布升级为 `20260805-visual-retry-consent-v40 / reference-director-v4`。
- 本地 `platform:upgrade:test` 全量退出 0，真实模型调用为 0。

## [10:11] 调整：视觉失败恢复按钮改名为“重新生成”
- `public/story-ad/views/assetCenterBillingRetry.js`：计费未知状态下的主按钮由风险条件文案改为明确操作名“重新生成”；一次性授权、计费未知锁和二次确认内容保持不变。
- `public/story-ad/views/assetCenterView.js`：拦截提示改为引导用户点击“重新生成”并在确认窗口中阅读和接受费用风险。
- `scripts/test-story-ad-visual-assets-sync-v21.js`：新增主按钮名称、旧误导文案移除和风险提示仍由确认窗口承载的回归断言。
- 前端发布升级为 `20260806-visual-retry-label-v41 / reference-director-v4`，重新生成 37 文件发布清单并刷新缓存版本。
- 定向回归、语法、发布完整性、边界门禁及完整 `platform:upgrade:test` 均通过；完整回归用时 267.6 秒，真实模型调用 0。

## [11:16] Bug：场景规格投影丢失与操作按钮状态误导
- `assetCenterView.js`：场景规划按钮静止时使用中性样式，仅在执行期间显示主操作态；已有场景时使用“重新建立场景规划”。
- `assetCenterBillingRetry.js`：生产入口统一显示“重新生成”，费用风险说明继续放在二次确认层。
- `projectBundleService.js`：工作区投影保留 `mode/layoutText/materialLightText/interactionText/negativeText`，兼容字段从权威组合字段派生。
- `sceneBindingService.js`：新增按 `space_id` 的权威规格合并；完整客户端提交保持权威，缺字段的旧客户端只补缺失字段，禁止跨场景混合。
- `deploy-story-ad-release.js`：补入 `sceneBindingService.js` 发布清单，避免测试文件已更新而生产运行文件遗漏；发布版本升级到 `20260806-scene-spec-authority-merge-v43`。
- 新增/扩充 11 场景投影、旧客户端恢复、跨场景隔离和发布清单回归；最终完整平台回归退出 0，用时 272 秒，真实模型调用 0。

## [11:40] 新增：人物资产卡与配件证据知识
- `src/services/seeds/character_asset_card.js`：新增人物资产卡视觉合同，以及配件复用/本地派生/模型增强三级证据与非阻断建议；文章内容按平台原子资产、checkpoint 和本地拼版能力改写。
- `knowledgeBaseSeed.js`：注册两条新种子知识；`test-new-story-ad-image2-realism.js` 增加结构、检索关键词、零模型派生和计费未知停止重试断言。
- `deploy-story-ad-release.js`：将知识索引、新种子和对应回归测试纳入发布清单；版本升级为 `20260806-character-asset-kb-v44`。
- 本轮没有修改 `personDossierService`、`dossierCompositeService` 或人物生成提示词，运行时生成路径保持 v43 行为。
- 定向、语法、发布完整性、人物档案边界和完整平台回归全部通过；本地完整回归用时 260.4 秒，真实模型调用 0。

## [12:39] 新增：通用知识策略运行时与发布闭包 v46
- 新增严格规则 Schema、生命周期、确定性编译器、任务快照、运行时注入与精简追踪；人物、场景、关键帧、视频生成及独立 QA 均消费对应策略，QA-only 规则不会触发付费重生成。
- 配件链路改为本地证据优先：普通明确配件使用已有图像裁切，只有结构关键配件才请求模型增强；成功资产和 checkpoint 保持复用。
- 分镜内容局部变化只将对应关键帧/视频片段标记过期并保留付费证据；镜头数量变化才清空全部片段，最终成片始终失效重组。
- 发布文件改为单一依赖闭包，新增覆盖前后端与测试的运行清单、构建原子性、启动哈希校验、部署后进程/哈希核验和失败回滚；版本升级为 `20260806-knowledge-runtime-v46`。
- 回归覆盖五种规则状态、最大 18 镜头、并发/缓存、种子不覆盖管理数据、无行业硬编码、局部失效、旧代码阻断与构建失败原子性；真实模型调用 0。

## [12:46] Bug：Windows 发布前回归启动失败
- 根因确认：Node 24 在 Windows 直接 `spawnSync('npm.cmd')` 返回 `EINVAL`，旧部署脚本只输出 stdout/stderr，因而诊断为空；失败发生在连接生产之前，服务器未修改。
- 部署前回归改为通过 `ComSpec /d /s /c` 启动 npm，并把底层 `result.error` 纳入错误信息；发布闭包新增 Windows 启动策略断言。
- 运行代码发生变化后版本升级为 `20260806-knowledge-runtime-v47`，重新原子构建 37 个静态文件和 294 个运行文件；本地实际运行哈希为 `2373d46bb4391409b379788535e3ee0ff8ae30bc7b96c3d1817c74f29993b5bd`。

## [13:02] Bug：知识性能测试依赖本地旧数据
- 生产发布后快速回归使用空 `OUTPUT_DIR` 隔离测试时，性能测试未显式 seed，导致“生产知识规则可编译”断言失败；部署脚本已自动回滚。
- 生产数据库只读核对确认 4 条运行规则均已存在且结构完整，业务启动种子正常；修复为测试在隔离库内显式执行 `ensureSeeded()`，消除对开发机旧 outputs 的隐藏依赖。
- 因测试运行闭包变化，发布版本升级为 `20260806-knowledge-runtime-v48` 并重新构建验证。

## [14:02] 优化：全局按钮高可见交互反馈 v49
- `public/story-ad/styles.css`：精确指针悬停增加 `translateY(-2px)`、高饱和描边/文字、强投影与光圈；主按钮使用更亮背景和 10×26px 阴影；补全按下、键盘焦点、执行中和减少动态效果状态。
- `scripts/test-story-ad-workspace-v6-ui-regressions.js`：新增悬停位移、主操作高对比、执行中可见性和 reduced-motion 回归合同。
- 发布版本升级为 `20260806-interaction-feedback-v49`，原子构建 37 个静态文件、294 个运行文件；发布完整性 11 项、闭包 295 文件及完整 `platform:upgrade:test` 全部通过。

## [15:27] Bug：计费未知粒度、部分资产可见性与跨分支止损 v52
- 新增 `generationBillingGuardService.js`：同任务/同 generation 付费图片单飞，首次计费未知后阻止尚未提交单元；按任务隔离，跨用户并发保持。
- `mediaAdapter.js`：DeyunAI 与通用图片请求统一经过任务级安全门，并区分“供应商已提交未知”和“本地未提交即停止”。
- `visualAssetBillingAuthorizationService.js`、`sceneGenerationCheckpointService.js`、路由与资产中心：人物和场景 checkpoint 统一列出、逐项授权、一次消费；移除单项人物按钮的全局计费锁。
- 新增 `subjectCheckpointProjectionService.js`，并复用既有场景 checkpoint 投影：只展示已完成本地资产，最大 48 项，失败候选不冒充成功结果。
- `subjectAssetBundleService.js`：记录 checkpoint 所属主体与输入档案；部分人物单项续跑可跨新请求键复用旧成功单元，完整档案主动重做仍建立新版本。
- 新增 `test-visual-asset-recovery-v50.js` 并更新既有失败恢复/同步回归；定向 5 项、多用户隔离、最大投影、人物档案套件、v3 全量、发布原子性和边界检查全部通过，真实模型调用 0。

## [15:46] Bug：聚合失败场景隐藏局部成功图 v53
- v52 生产只读核对发现人物部分 checkpoint 已恢复，但场景 checkpoint 聚合状态为 `failed` 时，投影器仍提前返回，已成功且已付费的单视图没有展示。
- `sceneCheckpointProjectionService.js` 改为以每个视图的 `succeeded` 状态为展示依据；聚合任务失败不再隐藏成功视图，失败候选仍不会冒充成功资产。
- 回归样例固定覆盖“场景整体 failed、主视图 succeeded、布局 failed”的真实边界，确保后续同类任务可保留成功结果。

## [16:06] Bug：工作台二次投影丢失场景 checkpoint 状态 v55
- 生产探针确认底层 `projectSceneAssets` 已得到场景 4 张成功视图，但 `projectBundleService.sceneAssets` 二次组装场景卡时没有传递 partial/checkpoint 字段，工作台无法识别为可续跑的部分资产。
- 工作台投影现保留聚合状态、成功/失败视图键、计费核对标志与验证信息；回归测试从失败 checkpoint 一直覆盖到工作台场景卡，防止只修底层、上层再次丢字段。
- 运行代码发生变化后发布构建拒绝复用 v54 build ID，按不可覆盖门禁升级为 v55。

## [16:25] Bug：失效标记误拦截后续新场景 checkpoint v56
- 生产时间线确认 `scene_assets` 于 8 月 5 日内容修订时失效，而目标场景 checkpoint 于 8 月 6 日生成；旧逻辑只看产物类型已失效，导致失效之后产生的新成功视图也被隐藏。
- `taskViewService` 将旧正式场景资产、失效前 checkpoint 和失效后 checkpoint 分流：前两者继续隔离，后者仅在时间晚于失效边界且场景 ID 属于当前 `scene_config` 时投影。
- 新回归覆盖旧正式资产不回流、失效前 checkpoint 不回流、失效后新 checkpoint 可恢复三项谱系边界；版本升级 v56。

## [17:34] Bug：故事输入事实权威与真人弹窗交互 v60
- 新增 `src/services/newStoryAd/briefAuthorityService.js`：行业无关地判定纯剧情/商业主体，识别现代与古代交替的双人物合同，提取明确地点，并校验资产计划不得丢人物或替换地点。
- `briefGoalAssistService.js`、`storyAdService.js`、`briefView.js`：AI 帮写只在原文后追加“传播目标补充”；纯故事结果若凭空添加商品、品牌、卖点、购买或转化会被模型结果校验拒绝。
- `contextBuilder.js`、`productAssetResolverService.js`、`projectBundleService.js`：无明确商品时使用 `narrative_story`，不产生商品主体或商品资产卡；明确填写、参考识别和材料广告继续按商业主体处理。
- `assetPlanService.js`、`blueprintService.js`：人物数量、时代关系和明确地点进入提示合同及结果门禁；纯剧情不再强制商品证明结构。
- `workspace.css`、`assetCenterPersonSources.js`：替换不存在的主题变量，增加实体面板/输入背景、明显焦点、背景模糊隔离、滚动锁、ARIA、Esc 关闭和焦点恢复。
- 新增 `scripts/test-story-ad-brief-authority-v57.js` 并纳入 v6 回归；最终发布构建 `20260806-story-brief-authority-v60`，37 个静态文件、297 个运行文件。

## [18:04] 新增：广告 / 剧情显式内容模式 v63
- `briefView.js`、`briefSettingsSummary.js`、`workspace.css`：增加广告/剧情双选卡；未选择时阻止帮写和生成，选中、悬停、焦点状态清晰可见，内容目标文案同时支持商品与纯故事。
- `briefAuthorityService.js`、`contextBuilder.js`：新增内容模式来源合同；只有用户亲自选择才允许继续，历史或参考自动推断只作建议且可被新证据纠正，避免旧项目兼容回归。
- `projectBundleService.js`：把内容模式和来源投影回工作台，已有项目可稳定回填。
- 回归新增显式模式优先、旧推断迁移、未选择门禁、AI 请求传输与选中态样式检查；v6 完整套件和 v3 全量套件均通过。
- 发布构建 `20260806-explicit-content-mode-v63`：37 个静态文件、297 个运行文件；行业硬编码分支 0，真实用户模型调用 0。
## [18:35] Bug：发布清单与 Git 快照换行符不可复现 → v64
- 三方逐文件核对发现：v63 本地与生产的 297 个运行文件哈希一致，但其中 146 个文本文件使用混合/CRLF 字节；Git `.gitattributes` 要求 LF，因此全新克隆会得到与生产不同的字节哈希。
- 根因位于发布构建前的工作树字节状态，而不是业务逻辑；将运行清单内文本文件机械规范化为 LF 后，业务语义 diff 为 0，并重新原子构建 `20260806-reproducible-handoff-v64`。
- 发布闭包为 297 个运行文件、298 个发布文件（含发布清单）；发布完整性、原子性、闭包、v6 边界及完整 v6/v3 回归全部退出码 0，真实付费模型调用 0。

## [20:12] 新增：多尺度场景卡知识运行规则 v65
- `generation_runtime_policy.js`：场景规则升级为 v2，把总览轮廓/边界、空间关系/机位、局部互动/资产组、材质/风化/氛围/细节组织为同一物理场景的四级证据；跨尺度锁定几何、资产清单、环境状态、灯光与色彩锚点，并继续禁止任务外行业模板和元素。
- `test-new-story-ad-knowledge-policy-architecture.js`：新增场景 v2 胜出旧版本、生成提示包含尺度梯度与色彩锚点、QA 核对同一物理空间的注入回归。
- 发布版本升级为 `20260806-scene-card-knowledge-v65`；知识策略专项三组、完整 `platform:upgrade:test`、发布构建/完整性/原子性/闭包均退出码 0，真实模型调用 0。
- 启动后本地知识库仍为 256 条文档，目标场景文档已合并 v1/v2；编译器按同冲突域优先级和版本只让 v2 进入新任务快照，旧任务不漂移。

## [22:10] 新增：完整场景档案卡与确定性 PNG 导出 v67
- `sceneDossierProjectionService.js`、`projectBundleService.js`：把场景五视图、结构锚点、几何、路线、道具摆位、材质、灯光、QA 与分镜引用投影为独立 `scene_card`；多场景严格按 scene/space ID 隔离。
- `sceneSpaceContractService.js`：修复重复规范化时清空 requested story states、interaction anchors、routes 和 prop placements 的最早数据丢失点；重复规范化现在保持幂等。
- `sceneDossierCard.js`、`sceneDossierExport.js`、`scene-dossier.css`：新增资产封面、完整档案、明确缺失态和 1800×2400 浏览器本地 PNG 合成；导出读取现有图片，不调用模型。
- `assetCenterView.js`、`assetCenterPlanningDetails.js`、`api.js`：接入场景卡/档案抽屉、Blob 读取、Esc 关闭和焦点还原；原场景世界、3D、360°、机位及路线入口保持不变。
- 新增场景档案专项回归并扩展 v6 UI 夹具与体积预算；完整平台回归首次暴露夹具依赖注入缺口，修复夹具后定向与完整回归均退出码 0。
