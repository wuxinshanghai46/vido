# VIDO 代码变更日志 - 2026-08-04

> 自动生成 · 项目助理维护 · 一天一文件

## [00:13] 新增与修改
- `src/services/newStoryAd/referenceUnderstandingService.js`: 新增跨行业参考理解 V6，输出故事总述、因果链、人物、场景、品牌作用、视听对齐及事实/推断/未知证据。
- `src/services/storyAdWorkspace/referenceUnderstandingConfirmationService.js`: 新增内容版本绑定、结构完整性校验、幂等确认与零生成调用门禁。
- `src/services/storyAdWorkspace/referenceUnderstandingProjectionService.js`: 收敛项目投影逻辑，避免 bundle 服务膨胀和重复转换。
- `public/story-ad/views/referenceUnderstandingView.js`: 新增第一步七类深度报告、证据定位与权威输入确认界面，按需加载。
- `public/story-ad/views/workflowDirectorNodes.js`: 新增通用导演台/动画节点、端口合同和同一 DirectorScene 打开能力，按需加载 3D 模块。
- `src/services/storyAdWorkspace/graphProjectionService.js`: 统一投影参考理解、人物/场景/商品、导演场景、动画和镜头关系，仅传 ID、版本和摘要。
- `src/services/storyAdWorkspace/directorSceneService.js`、`src/services/newStoryAd/shotReferencePackService.js`: 阻止陈旧场景或人物/商品版本继续进入逐镜参考包。
- `public/story-ad/store/projectStore.js`: 修复丰富故事摘要被短 logline 覆盖的问题，同时保留用户手工编辑内容。
- `scripts/deploy-story-ad-release.js`: 将一次性发布脚本收敛为按 build_id 隔离的通用原子发布脚本。
- `config/story-ad-release.json`: 发布版本升级为 `20260803-reference-director-v9 / reference-director-v2`。

## [10:36] 修复参考视频完成态回写与历史进度重放
- `src/services/newStoryAd/referenceAnalysisTaskSyncService.js`: 新增服务端终态回写、并发去重、用户手工内容保护和零模型资产投影。
- `src/services/newStoryAd/referenceVideoAnalysisService.js`、`src/routes/newStoryAd.js`: 分析完成后不再依赖浏览器轮询回写；终态读取失败不再把已付费完成结果变成失败。
- `src/services/storyAdWorkspace/authoritativeReferenceProjectionService.js`、`projectBundleService.js`: 首次打开项目直接读取权威分析记录，避免旧 `running` 状态和持续增长的历史耗时。
- `public/story-ad/store/projectStore.js`: 补齐 V6 `reference_understanding` 传输，终态先停止轮询再绑定，避免重复进度与重复写入。
- `src/services/newStoryAd/assetPlanService.js`: 广告目标优先保留完整 `generated_brief`，用户手工目标仍保持最高优先级。
- 新增并接入参考同步回归；发布版本升级为 `20260804-reference-task-sync-v10`。

## [21:20] 参考理解编辑与条件折叠闭环
- `public/story-ad/views/briefView.js`: 广告目标与成片设置仅在已选择参考视频/链接时自动折叠；无参考时展开，参考移除时恢复展开，并将可编辑参考理解报告前置。
- `public/story-ad/views/referenceUnderstandingView.js`、`reference-understanding.css`、`workspace.css`: 新增七分类参考理解编辑、保存、取消及编辑状态界面。
- `src/services/newStoryAd/referenceUnderstandingEditService.js`、`src/routes/storyAdWorkspace.js`: 新增服务端字段白名单、基础指纹与编辑版本并发控制，保存时保留证据并重投影故事、人物、场景和生成简报。
- `src/services/newStoryAd/revisionService.js`、`storyAdService.js`、`contextBuilder.js`、`referenceAnalysisTaskSyncService.js`: 用户编辑进入内容修订源，旧确认及 16 类下游结果失效；通用自动保存只允许原样携带已存覆盖，阻止伪造或变更覆盖。
- `scripts/test-reference-understanding-edit.js`、`scripts/test-reference-understanding-edit-persistence.js` 及既有 UI 回归：覆盖零模型调用、持久化、并发、确认失效、用户简报权威和条件折叠。
- 发布版本升级为 `20260804-reference-editable-brief-fold-v17`；完整平台回归、发布检查、边界检查和静态检查通过。

## [22:35] 360 场景权威、镜头同源与性能门禁
- `scenePanoramaService.js`、`panoramaProjectionService.js`、`panoramaProjectionWorker.js`: 新增 2:1 原始尺寸硬校验、视觉 QA、候选/发布状态、来源指纹、费用计划指纹、未决计费阻断、Worker 投影与哈希缓存。
- `shotReferencePackService.js`、`storyAdService.js`: 关键帧生成和场景 QA 优先选择同一权威全景的确定性派生机位，并兼容历史空场景资产。
- `sceneWorldService.js`、`projectBundleService.js`、`sceneWorldAssetProjectionService.js`: 投影全景权威与派生视图，能力改用结构化空间合同；普通图片、文件名或显式布尔值不能伪造 360/6DoF。
- `panoramaViewer.js`、`panoramaGeneration.js`、`sceneWorldView.js`: 新增点击后加载的原生 WebGL 3DoF 查看器、服务端动态费用预检、明确平面降级和 3DoF/6DoF 边界；取消隐藏 Canvas 和整页刷新。
- `pipelineModelService.js`、`modelCapabilityService.js`: 新增全景生成/QA、深度、空间重建/QA 阶段；深度与空间重建在无已验证供应商时保持关闭。
- `server.js`、发布脚本与版本门禁: 禁止生产环境关闭发布校验、禁止同 build_id 覆盖不同静态代码，版本升级为 `20260804-panorama-authority-v18 / reference-director-v3`。
- `test-new-story-ad-panorama.js` 及相邻回归: 覆盖假 2:1、坏接缝、旧权威保留、来源失效、并发/幂等、镜头与 QA 同源、行业无关、性能体积和旧链路兼容。

## [23:24] 参考确认连续流程与折叠交互修复
- `public/story-ad/views/briefView.js`、`briefSettingsSummary.js`: 折叠摘要展示真实广告目标、主题、时长、比例和分辨率；主 CTA 移到折叠区外，确认后按保存最新设置、零调用资产规划、进入资产中心的顺序执行，并用页面级 in-flight 防止双击重复提交。
- `public/story-ad/views/referenceUnderstandingView.js`: 确认按钮明确为“确认并创建人物与场景方案”，确认成功后通过回调继续主流程；同时明确这里只建立可编辑方案、不生成图片或视频。
- `public/story-ad/views/assetCenterView.js`、`workspace.css`: 新增跨行业通用的视觉生成下一步卡，显示缺失人物/动物与场景数量；付费人物、动物、场景图片仍分别在提交前确认，支持桌面与移动端布局。
- `scripts/test-story-ad-reference-confirm-continue.js` 及既有 UI 回归: 新增确认请求→刷新 bundle→继续回调顺序验证，覆盖折叠外 CTA、真实摘要、视觉付费边界和待生成场景识别。
- `package.json`、`scripts/deploy-story-ad-release.js`、发布清单: 新回归纳入本地与生产全量套件；版本升级为 `20260804-reference-confirm-flow-v20 / reference-director-v3`。
