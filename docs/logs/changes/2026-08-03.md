# VIDO 代码变更日志 - 2026-08-03

> 自动生成 · 项目助理维护 · 一天一文件

## [00:12] 修复: 拍摄细则、异步进度、主体参考与灯箱切换
- `public/story-ad/components/ui.js`：灯箱先用卡片缩略图即时渲染，再异步替换高清原图，并预载相邻原图；切换时图片、标题、计数和缩略图状态原子更新。
- `public/story-ad/store/projectStore.js`、`views/storyboardView.js`：提交 202 任务后立即合并新任务状态、清理旧阶段 100% 进度，不再用全量刷新打断轮询。
- `sceneCameraProjectionService.js`、`projectBundleService.js`：从机位合同投影结构化动态拍摄路线、器材、速度、时长、主体动作、焦点、起止状态、连续性与稳定方式，且持久化值优先。
- `assetCenterPlanningDetails.js`、`assetCenterView.js`、`workspace.css`：拍摄预案默认折叠；已有场景显示重新生成；商品和展示主体支持上传、独立商品多视图生成及材料/空间主体参考图生成。
- `productAssetGenerationService.js`：保留材料/表面不能伪装独立商品四视图的硬门禁，仅在显式 `reference_only` 时生成单张可复用主体参考图。
- 静态资源版本更新为 `20260802-shooting-lightbox-r29`；规划专项回归扩展至 79 项，新增异步进度、灯箱、动态拍摄和主体参考合同覆盖。

## [01:23] 修改: 人物与场景生成前权威补齐
- `generationSpecCompletionService.js`：新增人物服装六类与场景文本/结构化完整性检查、用户原文优先追加、失败前置阻断及指纹检查点复用。
- `subjectAssetBundleService.js`、`sceneAssetService.js`：在图片模型调用与完整性门禁之前执行补齐，并持久化到统一上下文和场景规划。
- `assetCenterView.js`、`workspace.css`：服装字段允许留空或只写明确要求，人物和场景生成确认中明确展示补齐规则。
- 新增人物/场景补齐回归；修正场景集成测试的文本模型隔离；模型能力审计基线更新为 58 个阶段。
- 发布脚本覆盖本次提交全部 96 个代码/脚本文件，并执行逐文件 SHA-256 核对。

## [11:22] 修复: 人物年龄、完整档案重生成与全局灯箱切换
- `projectBundleService.js`、`personTextProjectionService.js`：工作区投影修复被截断的实际年龄文本，不修改持久化原文。
- `assetCenterView.js`、`assetCenterPersonSources.js`、`personDossierShowcase.js`：移除重复年龄输入和内部占位值展示，统一为“外貌、气质与年龄”；完整人物入口明确重建身体、面部、表情、动作、服装、鞋履和饰品。
- `components/ui.js`、`workspace.css`：灯箱先预载新图、隐藏旧图并原子更新；动态主图增加媒体锁，阻止全局媒体优化器把切换后的地址恢复为第一张图。
- 静态资源版本更新为 `20260803-person-age-lightbox-r33`；增加年龄展示、完整人物重生成和全局灯箱根因回归，按需加载源码边界调整为 273 KiB，初始加载 100 KiB 门禁保持不变。

## [11:40] 修改: 生产清单防止新旧验证代码混用
- `scripts/deploy-2026-07-31-story-ad-v6.js`：把灯箱细节回归和生产规划回归纳入原子发布文件集，保证生产不会用旧测试验证新运行代码。
- 发布后核对 `public/story-ad` 本地/生产文件集合一致，旧缓存版本引用为 0，不需要删除额外业务文件。

## [15:30] 新增: 通用 SceneWorld 场景世界与新版门禁
- `sceneWorldService.js`、`storyAdWorkspace.js`：从现有人物、场景、分镜和机位权威数据投影行业无关的场景世界、人物×场景矩阵、观察点、动态机位、跨场景入口和生产清单，并增加带版本冲突保护的读取/保存接口。
- `sceneWorldView.js`、`assetCenterView.js`、`workspace.css`：资产中心新增场景世界工作区、生产清单、场景衔接和原生可旋转场景代理；支持模型、结构路线、人物站位、机位视图和点击入口切换世界。
- `server.js`、`api.js`、`bootstrap.js`：统一 build/contract 版本，旧剧情广告入口只跳转新版 `/story-ad/`，旧客户端写请求返回 426，避免旧页面覆盖新版内容。
- 人物完整档案、服装、鞋履、饰品与既有生成链路保持原权威数据流，本次只新增场景世界投影，不另存第二份人物或场景事实。
- 新增 SceneWorld 定向回归并纳入 V6；最终定向、V6、V3 全回归及真实浏览器旋转、模式切换和跨场景入口验收通过。
