# VIDO 代码变更日志 - 2026-08-05

> 自动生成 · 项目助理维护 · 一天一文件

## 事件流水

## [00:34] 修改
- `src/routes/newStoryAd.js`: 新增人物与场景联合生成入口，按独立通道执行并在结束时合并上下文，保留部分成功结果。
- `src/services/newStoryAd/visualAssetProgressService.js`: 新增联合生成父进度及人物、场景双通道状态。
- `src/services/newStoryAd/mediaAdapter.js`: 人物与场景模型调用统一进入共享并发池，避免并发失控和加载阻塞。
- `src/services/storyAdWorkspace/sceneWorldService.js`: 场景世界仅从已生成视觉资产的场景构建，并返回规划中与待生成数量。
- `public/story-ad/views/assetCenterView.js`: 主动作置顶并支持人物、场景同步生成；场景世界移至场景资产之后。
- `public/story-ad/views/sceneWorldView.js`: 未生成场景视觉资产时锁定世界观、360 与生产清单操作。
- `public/story-ad/components/ui.js`: 区分连接中断、计费待核对、内容审核与质量失败，不再把连接错误误报为审核失败。
- `scripts/test-story-ad-visual-assets-sync-v21.js`: 新增联合生成、门禁、并发池、错误分类和防重复响应回归测试。
- `scripts/deploy-story-ad-release.js`: 将联合进度服务和新增回归测试加入生产发布清单，防止线上缺模块或测试使用旧文件。

## [00:51] 新增
- `docs/handoffs/2026-08-05-visual-assets-scene-world-continuation-handoff.md`: 记录人物与场景联合生成、场景世界门禁、三方代码一致性、活动任务、验证证据、费用边界和公司电脑续接顺序。

## [01:00] 修改
- `docs/handoffs/2026-08-05-visual-assets-scene-world-continuation-handoff.md`: 补充联合任务终态、供应商 500 证据、6/6 进度口径缺陷、计费未知阻断和场景未提交事实。

## [09:59] 修改
- `public/story-ad/views/briefView.js`: 将广告目标与启动材料双栏移动到参考分析进度和参考理解报告之前，修正目标页信息层级。
- `scripts/test-story-ad-workspace-v6-ui-regressions.js`: 反转旧顺序断言，固定“设置在上、分析报告在下”的新规则。
- `config/story-ad-release.json`、`public/story-ad/release-manifest.json` 及前端缓存标识：发布版本升级为 `20260805-brief-inputs-top-v24 / reference-director-v3`。

## [10:08] Bug: 设置区仍不在用户指定位置 → 报告内部插槽闭环
- `public/story-ad/views/referenceUnderstandingView.js`、`reference-understanding.css`: 在确认操作区与完整故事摘要之间新增专用设置插槽，并在报告每次重绘后重新挂载设置节点。
- `public/story-ad/views/briefView.js`: 为广告目标与启动材料保留稳定节点和无报告时的恢复锚点，避免确认、编辑或刷新后节点消失或回到底部。
- `scripts/test-story-ad-reference-understanding-ui.js`、`test-story-ad-workspace-v6-ui-regressions.js`: 增加报告内部插槽、重绘恢复及回退锚点顺序回归。
- 发布版本升级为 `20260805-brief-settings-inline-v25 / reference-director-v3`。

## [10:16] Bug: 最终回归暴露 Windows 原子写入与发布边界问题 → 重试替换并收敛文件边界
- `src/services/newStoryAd/personDossierService.js`: 对 Windows 临时文件 `renameSync` 的 `EPERM/EACCES/EBUSY` 增加有限重试与复制替换兜底，确保人物档案并发持久化不会因短暂文件占用中断。
- `scripts/test-new-story-ad-person-dossier.js`: 增加一次模拟 `EPERM` 的回归用例，并连续三轮通过 54 项检查。
- `public/story-ad/views/briefView.js`: 保留报告内部插槽闭环的同时将源码收敛至 599 行，通过单文件 600 行发布边界。
- 发布版本升级为 `20260805-brief-settings-inline-v26 / reference-director-v3`；完整平台回归、定向 UI、发布完整性、模块语法及边界检查全部通过。

## [10:38] Bug: v26 生产启动被发布完整性门禁拒绝 → 最终文件形态重新生成清单
- `scripts/deploy-story-ad-release.js`: 将生产哈希核对拆为发布复制后、完整回归后、PM2 重载后三个阶段，能够定位最早发生变化的边界并保持失败自动回滚。
- 根因确认：v26 清单生成后又对 `reference-understanding.css` 做了 LF 换行规范化，导致清单记录旧哈希；上传、复制和回归后的原文件哈希均一致，但 PM2 启动完整性校验正确拒绝了旧清单。
- `config/story-ad-release.json` 与 34 个运行时静态文件：升级并重新构建 `20260805-brief-settings-inline-v27`，在最终文件形态下通过发布完整性、语法、UI 与边界检查。

## [11:24] Bug: 失败状态卡位于设置区下方 → 回退锚点顺序修复
- `public/story-ad/views/briefView.js`: 将参考分析进度/失败状态挂载点移动到设置区回退锚点之前；失败与分析中状态按“状态卡 → 广告目标与启动材料”展示，成功报告仍使用原有报告内部插槽。
- `scripts/test-story-ad-workspace-v6-ui-regressions.js`: 新增失败/分析中回退顺序和报告挂载点边界断言，防止再次把状态卡放到设置区下方。
- `config/story-ad-release.json` 及 34 个运行时静态文件：发布版本升级为 `20260805-reference-progress-priority-v28 / reference-director-v3`。

## [14:42] 新增与修改: 自适应参考分析、语义断点恢复和商品证明闭环
- `referenceEvidenceStrategyService.js`、`referenceEntityContinuityService.js`: 按镜头时长、证据意图和 40 帧预算动态取证；行业无关地跟踪多人物、动物和物理场景。
- `referenceSemanticRecoveryService.js`、`referenceVideoAnalysisService.js`: 五类语义合同逐步持久化，保留最佳候选、只修缺失合同；复用画面证据时独立恢复临时 ASR 失败，公开响应剔除大型私有缓存。
- `productIdentityContractService.js`、`assetPlanService.js`、关键帧/成片 QA: 区分商品视觉锁定与商品证明要求，支持独立商品、材料表面、空间成果和数字服务，最终合成前执行逐镜证明覆盖审计。
- `sceneGenerationPolicyService.js`、`productAssetResolverService.js`、`sceneAssetService.js`: 拆分场景升级策略；只有材料/表面商品参考图可作为场景材质，独立商品图不再冒充场景材料。
- 新增四组专项回归并纳入 V3；发布构建升级为 `20260805-adaptive-reference-recovery-v29`，34 个前端运行文件由单一清单统一版本化。
- 完整平台回归、V6 体积边界、发布清单、浏览器本地验收均通过；真实模型调用为 0。

## [15:46] 新增与修改：600 秒长视频生产链路
- `productionLimitsService.js`、`contextBuilder.js`、`blueprintService.js`、`storyboardTableService.js`: 统一目标时长上限为 600 秒，将长片拆成最多 18 个宏观章节和最多 120 个镜头；600 秒默认生成 100 个 6 秒镜头，并补充镜头连续性指令。
- `qualityReviewService.js`、`finalVideoQaService.js`: 分窗口审查全部分镜并按成片时长动态设置全流解码预算，避免只检查前段镜头。
- `ttsAdapter.js`、`storyAdService.js`、`routes/newStoryAd.js`: 语音按有限并发分批生成、逐批保存断点并复用已完成音轨；各生产阶段按目标时长动态计算超时预算。
- `composeService.js`: 使用最多 12 路输入的分层合成树，并在最终输出统一校正视频与音频时长；100 镜头合成不会形成单个超大 FFmpeg filter graph。
- `briefView.js`、`briefSettingsSummary.js`、`storyboardView.js`、`shotDesignerView.js`: 时长选项扩展至 180/240/300/360/480/600 秒，分钟格式展示；100 镜头按每页 20 条渲染。
- `referenceVideoAnalysisService.js`: 已完成证据批次不再重复调用三个完整候选，改为从语义检查点只补缺失合同；失败项目的继续入口复用 8/8 镜头证据。
- 新增 100 镜头/600 秒真实媒体分层合成回归，并扩充时长、语音断点、语义恢复、默认折叠和 UI 分页回归；发布清单纳入全部新增运行模块和测试，版本升级为 `20260805-longform-semantic-resume-v34`。

## [17:36] Bug：语义候选部分成功仍整体失败 → 五合同独立恢复
- `providerAdapterRegistry.js`、`modelGateway.js`: 支持 JSON Schema/JSON Object 结构化返回、能力降级、确定性 JSON 修复及可核对诊断元数据。
- `referenceSemanticContractPromptService.js`、`referenceSemanticRecoveryService.js`: 新增五类行业无关合同提示、字段所有权、独立候选合并、幂等尝试账本、旧检查点迁移与 512KB 有界压缩。
- `referenceVideoAnalysisService.js`: 复用已保存镜头证据，只按缺失合同修复；时间线与场景依赖按最早错误状态补齐，最终统一校验，不重复调用视觉模型。
- `referenceProgressCard.js`、`reference-progress.css`、`briefView.js`、`projectTimingProjectionService.js`: 展示五类合同状态、保留结果和当前修复项，锁定重复提交，并在刷新时投影小型进度摘要。
- 新增结构化输出、极限规模、真实生产失败记录离线回放和 UI 持久化回归；发布版本升级为 `20260805-structured-semantic-contracts-v35`。

## [17:44] 新增：v35 回家续接交接
- `docs/handoffs/2026-08-05-structured-semantic-v35-home-continuation-handoff.md`：汇总 v29/v34/v35 数据流、提交记录、138 文件三方哈希核对、真实 81% 新失败、费用边界和家庭电脑继续诊断顺序。

## [21:59] Bug：参考语义 81% 反复失败 → 权威基数、合同隔离与零调用重审
- `referenceEntityContinuityService.js`：人物身份签名移除画面位置，过滤/归并手部特写与跨镜同一人物；物理场景以环境身份为主，不再因景别、布局和光线变化过度拆分。
- `referenceVideoAnalysisService.js`、`referenceUnderstandingService.js`：逐帧证据权威决定人物数量和逐镜事件基数；部分模型结果只能补充语义，不能缩减或扩张权威集合；旧检查点按当前证据重审，已完成候选不重复付费。
- `referenceSemanticContractPromptService.js`、`referenceSemanticRecoveryService.js`：事件、人物、场景 schema 补齐业务必填字段；单合同失败继续执行后续合同，最终统一报告缺失项并保留所有成功片段。
- 五个专项测试增加生产 8/8 证据零调用回放、部分人物/事件不可覆盖、手部与跨镜身份、物理场景、强 schema 和合同失败隔离回归。
- `.gitattributes`、`config/story-ad-release.json`、`public/story-ad/release-manifest.json` 及发布静态文件：统一 LF 发布规则，版本升级为 `20260805-semantic-root-cause-closure-v36 / reference-director-v4`。
