# VIDO 会话日志 - 2026-08-05

> 自动生成 · 项目助理维护 · 一天一文件

## 当日概览
完成人物与场景联合生成、页面动作层级、场景世界门禁及人物模型失败语义的根因修复与回归验证，进入生产发布阶段。

## 事件流水

## [00:34] 用户反馈与根因闭环
- 用户要求下一步主动作统一置顶，生产清单与场景世界移至场景资产下方，并恢复人物与场景同步生成。
- 已确认旧流程由任务级单作业锁导致第二个生成请求被判重，前端又把未接受的重复响应当作成功；直接解除锁还会造成上下文和进度互相覆盖。
- 生产失败批次已确认是图片模型上游在响应头前断开连接；6 个素材已成功保留 2 个，其余调用计费状态未知，因此禁止自动重试，避免重复付费。

## [00:34] 开发与验证
- 新增 visual_assets 父作业，人物与场景以独立进度通道并行执行，场景内部串行，最终一次合并各自拥有的上下文字段。
- 场景世界仅消费已有视觉资产的场景；规划文本不再提前开启世界观、360 与生成清单功能。
- 调整主动作位置、失败分类与共享图片调用并发池，发布版本更新为 20260804-visual-assets-sync-v23。
- 最新代码上的完整平台回归通过；自动测试未发起真实模型调用。浏览器自动控制插件当前不可用，最终视觉交互仍需用户验收。

## [00:51] 用户需求与交接核对
- 用户要求确认服务器、Git 与本地运行代码一致后生成当天交接文件，供次日在公司电脑拉取继续优化。
- 本地、Gitee/Origin、GitHub 初检均为 `d93e60c2`，ahead/behind 为 0/0；本地服务运行 v23 且健康。
- 生产 110/110 个发布文件哈希一致，PM2、内外网健康、数据库和 SQLite 检查正常；本轮只读核对未触发模型、媒体或业务写入。
- 核对期间发现用户侧联合生成任务正在运行；人物已完成 6/6，场景仍等待响应。交接明确禁止重复提交，并把次日第一步设为只读确认该任务终态。

## [01:00] 模型失败终态诊断
- 联合任务最终因人物服装细节图调用返回供应商 HTTP 500 / `UNKXXXO004IFR` 而失败，平台分类为 `PROVIDER_5XX_AMBIGUOUS`；提交与计费状态未知，未自动重试。
- 六个核心人物单元均成功，另有两次配饰细节调用成功；失败发生在完整人物资产最终组装前，因此检查点保留但人物资产没有提交。
- 发现进度口径缺陷：页面 6/6 只覆盖核心人物单元，不包含后续必做细节和合成，导致用户看到 6/6 后仍失败；场景检查点为 0，未发现本轮场景模型提交。

## [09:42] 公司电脑同步、三方复核与测试门禁
- 公司电脑安全保留既有删除项、追加日志和未跟踪研究文档后，从 `826aec4` fast-forward 到 origin 最新 `a658e79f`，ahead/behind 为 `0/0`；三个重叠日志仅移除冲突标记并保留双方追加内容。
- 本地 3007 服务运行 `20260804-visual-assets-sync-v23 / reference-director-v3`；生产 PM2 online，内外网健康与数据库正常，SQLite `quick_check=ok`，活动生成任务为 0。
- Git 发布口径与生产共 110 个文件在统一换行后全部一致；34 个静态运行文件逐文件符合生产 manifest，manifest 规范化内容与本地一致。
- 完整 `platform:upgrade:test` 退出码 0，耗时 238.6 秒；CommonJS 72 个文件与前端模块 27 个文件语法检查通过，未触发真实模型或业务写入。
- 昨日任务仍为 `visual_assets_failed`，模型调用总数保持 14，计费状态 unknown；最新提交仅补充诊断文档，尚未修复 6/6 进度口径、分支终态同步和阶段性人物资产提交，因此撤回付费重试验收，当前不可测试该联合生成链路。

## [09:59] 用户反馈与目标页顺序修复
- 用户指出“广告目标与成片设置”位于参考报告下方不合适，要求移动到页面上方；模板与旧回归均锁定了错误顺序。
- 已调整为“顶部下一步操作 → 广告目标与启动材料 → 分析进度与参考理解”，真实本地页面 DOM 核对顺序为 `settings / progress / understanding`。
- 定向 UI、参考理解、联合生成边界、发布完整性和人物档案复跑均通过，未触发模型调用；完整平台回归分别遭遇参考缓存时序、FFmpeg 内存不足和 Windows 临时文件 `EPERM`，相关单项复跑均通过但完整命令尚未一次性退出 0，因此未提交、未推送、未部署生产，也未开放人物与场景付费生成。

## [10:08] 用户反馈与位置纠正
- 用户明确指出目标位置是参考理解报告确认操作区下方、完整故事摘要上方；此前“移动到报告整体上方”的理解错误已撤回。
- v25 改为报告内部专用插槽，报告重绘后自动恢复同一设置节点，无报告时回到目标页锚点；定向顺序、确认连续流程、语法和发布完整性检查通过。
- 生产仍未部署，人物与场景生成阻塞仍未修复，继续禁止付费测试。

## [10:16] 修复验收与待部署状态
- 用户指定的最终顺序锁定为“参考内容理解报告标题与确认操作 → 广告目标与成片设置/启动材料 → 完整故事摘要”，设置节点在报告重绘后仍保持该位置。
- 完整平台回归最终通过；此前可重复出现的 Windows 人物档案临时文件占用已在最早原子替换路径修复并补充模拟锁回归，`briefView.js` 也通过 600 行边界门禁。
- 本地发布版本为 v26，3007 健康接口返回 HTTP 200；本轮未触发真实模型调用。生产部署与部署后核对仍待执行，人物与场景付费生成仍保持不可测试。

## [10:38] 生产发布与浏览器验收
- v26 首次部署因本地命令超时留下孤儿锁，只删除经核对无进程且生产仍为 v23 的精确锁目录；后续两次发布均在失败后自动回滚，最终定位到清单生成后换行规范化造成 CSS 哈希陈旧。
- v27 重新生成清单并成功部署；生产 110/110 文件哈希一致，PM2 online，内外网健康、数据库和 SQLite 正常，活动生成任务 0。
- 真实生产项目页面只读验收确认纵向顺序为“确认操作 → 广告目标与成片设置/启动材料 → 完整故事摘要”，设置节点确实位于报告内部插槽；未点击生成，真实模型调用 0。
- 昨日失败人物任务的进度口径、通道终态和阶段性资产提交阻塞未在本轮修复，人物与场景付费生成仍不可测试。

## [11:07] 用户反馈与人物档案清晰度诊断
- 用户反馈人物完整档案在灯箱放大后模糊；截图显示当时加载尺寸为 720×960。
- 生产真实页面只读核对确认：720×960 是 `?thumb=720` 预览图，档案原图路径不带缩略参数，实际自然尺寸为 1800×2400；当前灯箱最终能够切换到原图，因此不需要重新调用模型或付费放大。
- 可优化点是将原图与预览并行预载、原图就绪前显示“高清原图加载中”并暂时限制放大，避免用户在预览阶段放大看到模糊画面；本轮只诊断，未修改代码。

## [11:24] 用户反馈与失败状态顺序闭环
- 用户进一步明确：参考视频分析失败/进度卡应直接位于操作方法下方，不能落在广告目标与启动材料下面。
- 已确认根因是无报告状态会恢复设置节点，而备用锚点固定在进度挂载点之前；修复后失败与分析中状态为“进度/失败卡 → 设置区”，成功报告内部的“确认操作 → 设置区 → 完整故事摘要”保持不变。
- 定向 UI、报告布局、确认继续、发布完整性、边界与完整平台回归通过；本地 v28 服务在 3007 返回 HTTP 200，测试未触发真实模型或付费生成。

## [11:27] 生产发布与真实页面验收
- v28 已提交、推送并部署生产；110/110 文件一致，PM2、内外网健康、数据库和 SQLite 正常，活动任务发布前后均为 0。
- 真实失败项目只读验收确认：参考视频分析失败卡位于操作方法下方、广告目标与启动材料上方；DOM 纵向坐标不重叠且顺序正确。
- 未点击重新读取、人物或场景生成，未产生新的模型调用；昨日人物与场景付费生成链路的既有阻塞不在本轮修复范围内。

## [11:40] 参考视频 7/8 批失败诊断
- 用户询问为何参考视频不能一次性全部识别完成；生产只读记录确认该任务共提取 32 张证据帧、分为 8 批，其中 7 批共 28 帧通过严格覆盖校验。
- 第 6 批的 F024 虽有返回编号但没有可用画面内容，主模型被判定逐帧证据不完整；备用视觉模型随后返回 429 限流，冷却约 5 分钟，因此任务安全停止且未进入人物、场景和剧情整理。
- 当前重试路径只读取缺失的第 6 批，不会重新调用已成功的 7 批；可优化点是把整批失败收敛到单帧补读，并在限流冷却后提供安全自动续跑，但本轮仅诊断、未触发重试或模型调用。

## [13:45] 参考视频语义整理三模型失败诊断
- 用户补读后仍失败；生产记录确认 8/8 视觉证据批次已经完整，第 6 批也由主视觉模型成功补齐，上一轮逐帧缺口已关闭。
- 新失败发生在 `reference_video_synthesis`：Gemini Flash 缺深度理解、叙事类型、完整故事/因果、人物、场景和品牌语义；Gemini Pro 缺深度理解、人物/场景、场景事件映射和品牌语义；GPT-4o 仅缺场景语义与场景事件映射。
- 根因路径是语义整理采用单次大合同并执行全有或全无校验，未保存或组合部分合格候选，也没有只修补 GPT-4o 剩余两个字段；语音转写同时为非阻塞失败，进一步减少对剧情因果和品牌功能的语义证据。
- 当前再次点击会重新触发语义候选调用，不能保证复用或只补缺失字段；本轮仅只读诊断，未触发模型、媒体或业务写入。

## [14:10] 参考分析、多主体投影与商品链路核对
- 用户要求明确一次性完整分析的修复方式，并确认多人物、多场景、宠物和商品在后续资产生成中的真实作用。
- 当前实现可将通过质量门的 `character_prompts`、叙事型 `animal_prompts` 和按物理空间合并的 `scene_prompts` 自动投影为人物、宠物和多场景草案；环境鸟群等非叙事动物不会生成宠物资产，投影只建方案而不会静默触发付费图片。
- 生产当前项目因参考语义整理失败，任务上下文中的人物、宠物、场景、商品事实均为空；商品字段仍为通用“当前广告主体”，没有 `product_asset`、商品参考图或可验证商品合同，因此后续无法锁定或展示具体商品。
- 商品链路分为商品语义、商品视觉资产/一致性合同、分镜商品出镜规则三层；人物与场景联合生成不包含商品资产生成。参考视频只能自动提取商品类别与故事职责，不能替代用户实际商品参考图。
- 推荐将参考整理拆为产品事实、人物/宠物、物理场景、故事因果、品牌职责等可独立校验的子合同，保存最优部分结果并只补缺失字段；语音转写独立恢复，最终再合并校验，从而实现用户一次点击而非单次大模型请求。

## [14:25] 镜头证据取帧与提示合同澄清
- 用户质疑“32/32”是否是固定取帧和固定提示；代码核对确认 32 不是全局固定值，而是当前 60 秒视频检测到 16 个取证片段后，每段取开头/结尾两帧得到的结果。
- 固定参数包括场景切换阈值 0.4、最小切换间隔 0.75 秒、最长取证片段 6 秒、最多 40 帧、每批 4 帧；片段不超过 20 个时每段取 2 帧，否则每段只取中点代表帧。
- 当前逐帧视觉提示合同基本固定，只动态传入帧编号、时间、镜头区间和开头/结尾角色；没有按商品特写、人物关系、宠物动作、场景建立、动作峰值或转场类型切换专项提示与增补取帧。
- `32/32` 仅证明预先选中的 32 帧都取得结构化响应，不等于视频的全部镜头事件、主体变化和语义均已覆盖；推荐升级为两阶段自适应取证、按镜头意图路由专项提示，并以镜头/事件/人物/场景/商品覆盖率替代单一帧计数。

## [14:42] 用户决策与完整开发验收
- 用户要求按完整方案一次做完参考分析、多人物/宠物/场景投影和商品作用链路，并要求控制代码体积、覆盖所有行业、完成全量测试后再由用户验收。
- 已将取证改为按镜头时长和意图动态规划；语义整理拆为可持久化子合同，保留最佳候选并只修缺项；临时语音转写失败可独立恢复，永久鉴权错误不重复调用。
- 人物计数区分全片去重人数和最大同屏人数，宠物与环境动物分离，多物理场景按权威镜头目录投影；商品区分独立商品、场景材料、空间成果和数字服务，并以逐镜商品证明合同进入关键帧、成片和最终 QA。
- 场景升级策略和商品材质引用拆出主服务，场景服务降至 1809 行；前端 Store、Brief 均保持 600 行边界，发布前端 34 文件统一升级为 v29。
- 定向回归、发布完整性和完整 `platform:upgrade:test` 均通过；完整回归耗时 234.4 秒，未调用真实供应商模型。本地 3007 服务健康，浏览器确认实际加载 v29 且首屏布局正确。

## [14:51] v29 生产发布与最终只读验收
- 提交 `f25a1c7` 与发布清单修复 `9ab9c7e` 已推送；首次发布因服务器仍使用旧场景材质断言而自动回滚，补齐测试脚本后第二次原子发布成功。
- 生产 123/123 发布文件哈希一致，release manifest 与本地 SHA-256 同为 `2853d05b...f6da`；PM2 online，内外网健康、数据库和 SQLite 均正常，活动任务 0。
- 真实失败项目仍保留原 8/8 镜头证据和旧语义失败状态，没有自动续用错误结果；生产页面加载 v29，并提供“继续补齐语义结构”入口。
- 只读验收未点击重试、人物、场景或商品生成，真实模型调用为 0；下一步由用户执行一次完整验收。

## [15:46] 用户决策与长视频验收范围
- 用户要求目标时长不能止于 120 秒，需提供 5—10 分钟选择，并要求长时长不是仅修改界面，而是完整覆盖分析、镜头、语音、合成与质检链路。
- 本轮将目标上限扩展到 600 秒；600 秒按 6 秒镜头规划约 100 镜头，采用章节批处理、语音断点续跑、分层合成和分页渲染，避免一次性大对象和大 DOM 导致卡顿。
- 已完成 600 秒本地真实媒体合成与全流解码、完整平台回归和浏览器页面核对；未自动发起 100 个真实付费视频模型调用，避免未经用户确认产生大额费用。

## [16:40] 用户反馈与参考语义恢复二次失败诊断
- 用户执行“继续补齐语义结构”后，8/8 镜头证据被正确复用，失败仅发生在 `reference_video_synthesis`，本次实际调用两个候选模型。
- Gemini 2.5 Flash 返回的结构化补丁仍缺人物合同 `cast` 与场景合同 `scenes`；Gemini 2.5 Pro 返回 3212 字符，但不是 JSON 且不是审核拒绝，因此均被严格合同校验拒绝。
- 已确认不是视频读取、网络、鉴权、限流或镜头证据失败。代码根因是语义修复仅通过提示要求 JSON，OpenAI 兼容适配器未传递强制 JSON/schema 参数，同时在 JSON 修复前先拒绝非 JSON 文本；两候选预算耗尽后任务停在 55%。
- 撤回参考分析可测试结论并要求停止重复点击，避免继续产生无收益的文本模型调用；长时长成片链路不受此问题影响。

## [17:36] 开发与验证：结构化语义合同断点恢复
- 将参考视频语义整理拆为故事、时间线、人物/宠物、物理场景、品牌/声音五类行业无关合同；每类独立保存最佳片段、失败模型与完成状态，只补缺失合同，不再把部分成功整体丢弃。
- 供应商适配层新增 JSON Schema/JSON Object 结构化输出，并在供应商明确不支持时安全降级；本地确定性解析先处理代码围栏、说明文字、尾逗号和截断，再决定是否调用修复模型。
- 真实生产失败记录离线回放复用 8/8 证据批次，镜头重新读取为 0，语义修复模拟调用 2 次，最终 5/5 合同完成；未触发真实供应商或付费生成。
- 前端新增五类合同进度、已保留/缺失状态和重复点击锁；项目刷新继续保留进度，浏览器确认 v35 页面实际加载且未出现旧版本拦截层。
- 定向回归、304.2 秒完整平台回归、发布完整性、V3/V6 边界和语法检查均通过；生产部署与部署后核对待执行。

## [17:48] v35 生产发布与三方核对
- 功能提交 `e61581f` 已推送 Gitee/Origin；本地与远端分支 ahead/behind 为 0/0，用户既有删除、旧日志换行和未跟踪研究文档未纳入提交。
- 生产已原子发布 `20260805-structured-semantic-contracts-v35 / reference-director-v3`，服务器完整回归通过，138/138 文件哈希一致，发布前后活动任务均为 0。
- 独立核对确认本地与生产 release manifest SHA-256 同为 `de673575...14b6c`，PM2 online，内外网 HTTP 200，数据库 ok，SQLite quick_check ok。
- 旧失败分析仍为 failed/55%，8/8 视觉批次完整且不在活动任务中；部署没有自动续跑、没有触发真实模型或付费媒体调用。旧 v1 语义检查点将在用户主动验收时按已通过回放测试的迁移路径转换，不直接当作完成结果复用。

## [17:44] 回家续接三方核对与新失败记录
- 用户要求确认服务器、Git、本地一致并生成可在家庭电脑拉取的交接文件；本地与 Origin/Gitee 为同一提交、ahead/behind 0/0，生产 138/138 发布文件逐一哈希一致，PM2、内外网、数据库、SQLite 正常，活动任务 0。
- 核对期间发现用户已执行一次 v35 真实验收：原任务更新为 failed/81%，8/8 视觉证据仍完整，三家语义候选均为 `PROVIDER_RESPONSE_INVALID`，旧 v1 语义检查点没有留下合同失败明细。
- 撤回继续重复测试结论；回家后先补脱敏供应商诊断和失败检查点持久化，再决定适配器或解析修复，不增加盲目候选重试。
- 新增 `docs/handoffs/2026-08-05-structured-semantic-v35-home-continuation-handoff.md`，记录完整数据流、提交、三方核对、验证、费用边界和续接顺序。
