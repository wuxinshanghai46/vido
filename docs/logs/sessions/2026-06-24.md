# VIDO 会话日志 - 2026-06-24

> 自动生成 · 项目助理维护 · 一天一文件

## 当日概览
AI 小说章节目录布局继续校准：从整行等高改为对齐正文文本框底边，同时保持章节目录内部滚动。

## 事件流水

## [18:07] 用户需求
- 要求账号选择颜色主题后跨浏览器保持一致。
- 要求修复剧情广告制作控制区域一边深一边浅的问题，输入框保持同一色调。
- 询问“Controlled”状态徽标用途，若无必要则去掉。
- 授权下载 `lijigang/ljg-skills` 并将其中“去 AI 化”表达方法融入项目知识库，应用到人物、小说、视频等涉及 AI 调用的提示词体系。

## [18:07] 处理结果
- 统一主题保存入口：主题切换会写入账号偏好，旧的 `vidoTheme.store` 调用也兼容为账号级保存。
- 剧情广告制作控制去掉英文 `Controlled/Classic` 胶囊，改为中文“展开/收起设置”，并保留增强控制链路含义。
- 浅雾蓝主题下制作控制卡片、下拉框、输入框、文本域统一为同一套浅色调、边框和聚焦状态。
- 以 `lijigang/ljg-skills` Markdown 分支为参考，新增项目自有“去 AI 化表达”知识库 seed；规则为原则型，不写死具体剧情、人物、年龄、服装或文案。
- 已部署线上并验证健康检查与知识库检索通过。

## [18:22] 用户需求
- 优先处理剧情广告人物生成结果差异化过大的问题，截图显示人物包候选图被 QA 拒绝，原因包含视觉 QA JSON 格式异常或匹配字段较弱。
- 要求把 `lijigang/ljg-skills` 参考内容放到服务器并真正应用到项目。
- 完成后同步到 git，并检查服务器、git、本地三方代码是否一致，方便回家后拉取继续优化。

## [18:22] 处理结果
- 修复视觉 QA 畸形 JSON 修复器：新增人物设定匹配字段与一致性字段识别，避免 `gender_match`、`wardrobe_match`、`hair_makeup_match` 等字段因 JSON 格式轻微异常被丢失。
- 人物包生成 prompt 新增“当前人物设定锁”：把用户填写的人物身份、地域/种族、年龄、外貌、服装、发型妆造和禁止项放到靠前硬约束，减少生成结果自由漂移。
- 将 `lijigang/ljg-skills` Markdown 参考快照纳入项目目录，后续会随 git 拉取；线上仍使用项目自有 KB seed 生效，不让业务运行依赖外部仓库。

## [00:01] 反馈
用户澄清 AI 小说章节目录应与中间“正文文本框”的内容区域底边平行，不是恢复成整行等高；左侧章节目录平行后仍需要可滑动。

## [00:01] UI: AI 小说章节目录高度 → 对齐正文文本框
- 新增前端测量逻辑，根据正文文本框底边与章节目录顶部的实际距离设置目录高度。
- 保留章节目录内部滚动，窗口尺寸变化或正文框尺寸变化时重新对齐。

## [00:11] 核查
- 本地 `master`、Gitee `origin/master`、GitHub `github/master`、生产服务器 `/opt/vido/app` 均为提交 `a1d2ad9`。
- 本地和生产的 tracked 工作区无未提交代码改动；本地存在历史未跟踪备份/临时文件，未纳入代码同步范围。
- 本地开发服务和生产公网 `/api/health` 均返回 200；AI 小说公网 HTML/JS/CSS 版本为 `20260623-chapter-list-text-align`。

## [00:44] 反馈
用户要求 AI 小说字数统计改为含标点口径，减少与 WPS 字数统计的差异。

## [00:44] 修改
- 小说章节字数统计统一为“保留标点、排除空白/换行”的口径。
- 前端显示、导入分析、后端保存和数据库归一化入口同步改为同一统计规则。

## [01:11] 反馈
用户要求 AI 小说章节增加删除功能：只有未提交本章的章节允许删除，删除后后续章节需要自动顺延补位，不能留下章节序号空洞。

## [01:11] 修改
- AI 小说章节目录增加未提交章节删除入口；已提交章节前端不显示删除入口，后端也拒绝删除。
- 删除章节时同步重排后续正文、剧情大纲章节、章节任务、审稿记录、人物关系、伏笔和记忆等关联章节编号。
- AI 生成章节默认保存为草稿，只有点击“提交本章”后才写入提交时间，避免“自动保存”和“正式提交”混在一起。

## [01:37] 反馈
用户确认已提交章节不能删除需要作为硬规则；随后追加需求：AI 小说章节目录支持拖拽移动章节，移动后章节序号和剧情大纲等关联数据同步调整；每章增加错别字检测和敏感词检测，命中内容需要标红并提示数量。

## [01:37] 修改
- 章节提交中和已提交状态均锁定删除入口，防止提交过程中误删。
- 章节目录支持拖拽排序，后端按章节编号映射同步重排正文、剧情大纲、章节任务、审稿记录、事实记忆、人物关系、伏笔等关联数据。
- 新增章节错别字检测和敏感词检测按钮；检测不保存、不改正文，只在结果预览中标红命中位置并提示命中数量。
- 新增可维护的小说敏感词知识库 seed，检测服务优先读取知识库扩展词条，并保留基础词库保证功能可用。

## [01:49] 反馈
用户纠正：已提交章节也需要允许删除；敏感词检测点击时需要先弹窗说明检测内容，检测结果也需要用弹窗展示给用户查看。

## [01:49] 修改
- AI 小说章节删除改为已提交章节也显示删除入口并允许后端删除；只保留“提交中不能删除”的并发保护。
- 敏感词检测点击后先弹出检测范围说明，确认后再检测；检测完成后弹窗展示命中数量、命中词分类和标红预览。
- 错别字检测结果同样弹窗展示，保留下方标红预览和 toast 提示作为辅助反馈。

## [02:00] 反馈
用户反馈 AI 小说章节移动后的提示在右下角太低，用户难以看到；章节拖拽交互不够明显，需要更清楚的移动状态和动画效果。

## [02:00] 修改
- 章节拖拽移动成功后改用页面中间浮层提示，明确告知“第 X 章已移动到第 Y 章，剧情大纲已同步调整”。
- 章节拖拽过程增加列表排序状态、拖动卡片脉冲阴影、目标卡片偏移高亮和落点线动画。
## [02:09] 反馈
用户反馈 AI 小说章节移动后的居中提示样式过重：浅色主题下不应出现深色完成徽标，提示语义也不应写成“已完成”，应按当前主题显示为轻量提示。

## [02:09] 修改
- 章节移动居中提示改为纯文本状态提示，不再渲染“已完成”徽标。
- 居中提示样式改为主题感知：浅色主题使用白底轻边框和浅阴影，深色主题保留深色体系但降低视觉重量。

## [02:09] 反馈
用户反馈 AI 小说章节移动后的居中提示样式过重：浅色主题下不应出现深色完成徽标，提示语义也不应写成“已完成”，应按当前主题显示为轻量提示。

## [02:09] 修改
- 章节移动居中提示改为纯文本状态提示，不再渲染“已完成”徽标。
- 居中提示样式改为主题感知：浅色主题使用白底轻边框和浅阴影，深色主题保留深色体系但降低视觉重量。

## [02:12] 记录修正
上一条日志追加时受 PowerShell 反引号转义影响，部署记录显示异常；本条补充正确要点：章节移动提示已改为主题感知的纯文本轻提示，浅色主题为浅色弹窗，不再显示“已完成”。

## [02:37] 反馈
用户补充确认：AI 小说章节拖拽排序必须适用于所有小说、所有章节，不是针对某一部小说；移动时不能只换章节名，当前章节的正文、故事点、大纲、提交状态和相关记录都要作为同一个章节实体一起移动。

## [02:37] 修复
- 前端拖拽前先同步并保存当前编辑草稿，同时把被拖动章节的标题、正文、状态和故事点快照随排序请求提交，避免刚编辑的内容在移动时丢失。
- 后端排序接口在重排前合并当前草稿和被拖动章节快照，并统一重排章节正文、剧情大纲、章节提交记录、事实记忆等关联章节编号。
- 修复 SQLite 查询缓存失效范围，确保小说更新后 `get:novels|id` 缓存立即清理，避免保存后立刻拖拽读到旧小说对象。

## [09:43] 检查
- 按用户要求启动项目、从 Gitee `origin/master` 快进同步本地，并连接生产服务器核对代码一致性。
- 同步前将本地未提交/未跟踪现场保存到 stash `pre-sync-2026-06-24-project-start`，避免覆盖本地改动；同步后本地 `master` 与 `origin/master` 均为 `366bdc74f565933a075dc245a4330ae0ead02812`。
- 本地开发服务已按 `PORT=3007` 启动，`http://localhost:3007/api/health` 返回 200。
- 生产服务器 `/opt/vido/app` 当前 `HEAD` 为 `366bdc74f565933a075dc245a4330ae0ead02812`，tree 为 `8b3926ef0688077ebd3182a7db8fd6d65b2b5c4b`，与本地一致；tracked diff 为空，tracked 文件数 563。
- 生产服务器存在大量历史备份/运行产物未跟踪文件，未计入代码一致性；服务器本地 `origin/master` 引用因 Gitee HTTPS 凭据不可用未能 fetch 更新，但生产 `HEAD` 已与本地远端最新提交一致。
- 生产 PM2 `vido` online，生产本机和公网 `/api/health` 均返回 200；数据库业务用户只读连通性 `SELECT 1` 成功。未记录任何凭证。

## [09:58] 检查
- 排查线上剧情广告“场景配置生成失败”：生产日志显示 `luxury_ad.scene_config` 在 `deyunai/gemini-2.5-pro` 返回 `400 status code (no body)` 后中断。
- 线上有效模型队列实际包含 `deyunai/gemini-2.5-pro`、`deyunai/claude-sonnet-4-6`、`deyunai/gpt-4o`、`deyunai/gemini-2.5-flash`、`deepseek/deepseek-chat`；不是配置只剩一个模型。
- 最小化探测确认漫路多个候选当前不可用：gemini/gpt 返回 400 no body，claude 返回 Insufficient quota；deepseek-chat 可正常返回。
- 初步根因：上游漫路文本模型/额度异常叠加代码 fallback 判定漏掉 `400 status code (no body)`，导致第一候选失败后没有继续切到可用 deepseek。
- 截图中的 `/api/dh/videos/tasks`、`/api/avatar/tasks` 404 属于轮询旧接口缺失/兼容问题，和本次 `scene_config` 生成失败不是同一主因。

## [10:04] 检查
- 按用户要求测试将剧情广告场景配置改用 `deyunai/gpt-5.5`：最小化 LLM 探测仍返回 400。
- 进一步用生产服务器直接 curl 漫路 `/c35/v1/chat/completions`，确认 `gemini-2.5-pro` 与 `gpt-5.5` 均返回 HTTP 400，业务码 `1005 Insufficient quota`，并分别带上游 request_id。
- 结论：当前不是单一 Gemini 模型故障，漫路海外 Chat 通道对应模型额度/授权不可用；切到 `gpt-5.5` 当前也不可用。未记录任何凭证。

## [10:49] 检查
- 用户确认供应商已充值后，复测漫路 `/c35/v1` 海外 Chat 通道。
- 生产直接 curl：`gemini-2.5-pro` 与 `gpt-5.5` 不再返回 Insufficient quota；`gpt-5.5` 返回 HTTP 200。
- 通过平台 `callLLM` 真实入口复测：`deyunai/gemini-2.5-pro` 与 `deyunai/gpt-5.5` 均成功返回 JSON 内容；说明充值后主因已解除。
- 仍建议后续补强 fallback：把 `400 status code (no body)` 也作为可切换候选模型错误，避免供应商下次额度/通道异常时卡在第一候选。

## [10:58] 修改与部署
- 针对线上剧情广告人物来源表单需要手动填写过多的问题，新增“AI 补齐人物设定”入口。
- AI 辅助会基于广告需求、人物数量、性别、年龄、地域等已有信息，自动补齐人物身份、姓名、外貌气质、服装、发型妆造和禁止项；默认只填空字段，避免覆盖用户已手写内容。
- 已同步到生产静态文件并验证线上健康检查 200；生产备份目录为 `.codex-backups/lux-person-ai-fill-20260624105718`。

## [11:46] Bug: 剧情广告人物生成刷新回模块首页 → 已修复上线
- 用户反馈昨天已处理“任意页面刷新停留当前页”，但剧情广告进入人物生成/人物来源后刷新仍回剧情广告首页。
- 根因：数字人路由只保存 `tab` 和 `lux_step`，没有保存剧情广告第 2 步内部的“人物来源/演员包生成”焦点；刷新后只能恢复到模块或步骤，不能回到人物生成区域。
- 修复：新增 `lux_focus=person` 子视图路由；人物设定输入、AI 补齐人物设定、上传真人参考、选择演员库、生成 AI 拟真演员包都会写入该焦点；刷新后恢复到第 2 步并滚回人物来源区域。
- 已同步生产 `public/digital-human.html` 和 `public/js/digital-human.js`，生产备份目录 `.codex-backups/lux-person-route-focus-20260624114651`，健康检查 200。

## [11:53] 修改与部署
- 按用户截图要求，将剧情广告“制作控制”右侧状态与展开入口升级为更高级的控制台样式：状态胶囊、发光状态点、玻璃质感背景、柔和阴影和浅色主题适配。
- 更新 `public/digital-human.html`、`public/js/digital-human.js`、`public/css/digital-human-wizard.css`、`public/css/vido-ui.css`，并同步到生产静态文件；未重启 PM2。
- 验证：本地 `node --check public/js/digital-human.js` 通过；本地 `/api/health` 返回 200；生产 `/api/health` 返回 ok。

## [12:01] 修改与部署
- 优化 AI 小说章节目录切换体验：点击章节后立即切换显示，不再等待上一章自动保存完成；当前章正文和故事点会先写入内存快照，再后台保存。
- 增强章节目录的点击指向效果：hover 位移、箭头提示、选中高亮条、选中脉冲和浅色主题下更明显的当前章节状态。
- 已同步 `public/ai-novel.html`、`public/js/ai-novel.js`、`public/css/ai-novel.css` 到生产；未重启 PM2。
- 验证：本地 `node --check public/js/ai-novel.js` 通过；本地 `/api/health` 返回 200；生产 `/api/health` 返回 ok。

## [13:30] 修改与部署
- 继续优化 AI 小说章节目录体验：章节拖拽排序改为本地乐观重排，松手后立即显示新顺序，不再等待自动保存和后端重排接口完成。
- 重排前仍会把当前编辑章正文和故事点写入快照，并把被移动章节快照提交给后端；后端完成后同步真实数据，失败时回退并提示。
- 已同步 `public/ai-novel.html`、`public/js/ai-novel.js`、`public/css/ai-novel.css` 到生产；未重启 PM2。
- 验证：本地 `node --check public/js/ai-novel.js` 通过；本地 `/api/health` 返回 200；生产 `/api/health` 返回 ok。

## [13:38] Bug: 剧情广告 AI 补齐人物设定误报缺少广告需求 → 已修复上线
- 用户反馈点击“AI 补齐人物设定”时提示“请先填写广告需求”，但页面已有上下文或已选择人物字段。
- 根因：前端只读取内部 `state.luxuryAd.text`，没有优先读取当前页面的广告需求输入框，状态未同步时会误判为空。
- 修复：AI 补齐入口优先读取 `#dhLuxAdText`；没有广告需求时也允许按当前人物数量、性别、年龄等选项补一版通用设定，不再阻断。
- 同时优化人物来源交互：AI 辅助按钮提升为标题右侧主按钮，“未选”状态改为横向“未选择”徽标，避免文字挤压变形。
- 已同步生产静态文件；未重启 PM2。验证：本地 JS 语法检查通过，本地 `/api/health` 200，生产 `/api/health` ok。

## [13:52] Bug: 剧情广告刷新露出生成形象/回广告需求 → 已修复上线
- 用户反馈：刷新 `tab=luxury-ad&lux_step=2&lux_focus=person` 后会回到剧情广告广告需求页，且加载慢时左侧先显示“生成形象”选中。
- 根因：首屏初始化先等待用户信息等接口，再恢复 URL 路由；同时第 2 步会因广告需求为空被步骤守卫夹回第 1 步，导致慢网时默认状态外露。
- 修复：首屏脚本提前读取 `lux_step`/`lux_focus`，JS 在任何网络请求前立即选中剧情广告和目标步骤；`lux_focus=person` 允许直接恢复人物配置区，不再被空广告需求夹回广告需求。
- 性能：用户信息、我的形象、任务状态、引擎状态改为后台/空闲加载，先让当前页面可见可操作。
- 已同步生产静态文件；未重启 PM2；本地和生产健康接口均返回 ok。

## [14:02] 修改与部署
- 用户反馈剧情广告人物来源区域：穿着字段是否会影响演员服装、生成拟真演员按钮不可点、按钮文案要去掉 AI、人物设定内容显示太窄看不全。
- 调整：按钮文案改为“辅助补齐”“生成拟真演员”；生成拟真演员不再强制要求先生成场景配置，有广告需求或已有任意人物设定即可点击；没有广告需求时会按当前人物设定生成通用拟真演员。
- 布局：人物设定改为更宽的平铺栅格，外貌、穿着、发型、禁止项改为多行输入框，并明确“穿着/服装”会进入演员包生成约束。
- 已同步生产静态文件；生产健康接口返回 ok。

## [14:05] 修改与部署
- 用户反馈剧情广告人物配置区所有功能按钮中文字需要居中对齐。
- 调整：统一数字人页面 `.dh-btn` 基础样式，补齐水平居中、垂直居中、行高和文本居中；人物配置按钮行显式居中；浅色主题覆盖层同步居中规则。
- 已同步生产静态文件；生产健康接口返回 ok。

## [14:16] Bug: 错误提示颜色不清晰与额度文案误导 → 已修复上线
- 用户反馈：浅色主题下人物演员包失败卡片白字在粉色背景上看不清；错误提示里写死“不等同于账户总余额不足”会误导，今天实际出现过余额/额度问题。
- 排查：人物演员包接口返回 `PROVIDER_LIMIT_EXCEEDED`，完整回执中上游模型 `gpt-image-2` 返回 `Insufficient quota`；前后端均存在把额度/频率限制解释成“不等同于账户总余额不足”的硬编码文案。
- 修复：额度/频率/余额/授权相关错误不再否认余额问题，主文案保留真实 code/message，操作建议要求按完整回执检查 provider、model、status、供应商原文、余额/额度/频率/分组授权。
- 样式：浅色主题下失败卡片、摘要、完整错误回执和 JSON 区域改为深色文字与浅色背景，保证可读。
- 已同步生产并 reload PM2；生产健康接口返回 ok。

## [14:34] Bug: 人物演员包失败提示颜色与无候选图原因泛化 → 已修复上线
- 用户反馈：浅色主题下人物演员包失败提示仍有白字看不清；“没有候选图”提示泛化为审核拒绝或无图，和完整回执中的真实错误不一致。
- 排查：生产日志命中 request_id `54FCE4C8804E5B3F87EF16999140FE04`，真实失败为 `deyunai/gpt-image-2` 返回 HTTP 400，供应商原文 `Insufficient quota`；本次不是前端接口问题，也不是审核拒绝或返回图片不可访问。
- 修复：失败卡片基础样式改用主题变量，不再硬编码白字；无候选图说明按错误类型生成，额度/余额/频率/授权限制时明确说明对应模型通道没有产生候选图。
- 已同步生产静态文件；生产健康接口返回 ok。

## [15:21] 用户需求
- 调整剧情广告人物来源动作按钮：辅助补齐与生成拟真演员放在同一动作区，默认使用普通按钮色，只有点击/生成中才显示主色。

## [15:28] 用户需求
- 用户确认人物来源按钮字体需要统一，并强调人物设定辅助不能固定模板，必须按不同剧情生成不同内容。

## [15:57] Bug: 剧情广告人物设定辅助补齐 502 → 已修复上线
- 用户反馈“辅助补齐”返回 502，页面提示人物设定补齐模型没有返回可用字段。
- 排查确认接口第一轮 LLM 被知识库示例污染，模型把无关人物示例当作当前广告上下文并返回英文分析/Markdown，导致后端解析不到有效字段。
- 修复为人物设定补齐接口跳过知识库注入，只使用页面当前广告需求、场景摘要、素材和已有字段；同时增强模型返回 JSON 提取和修复提示，不写固定兜底内容。
- 生产内部探针验证 `/api/dh/luxury-ad/person-spec/assist` 返回 200，并生成非空人物设定字段。

## [16:08] Bug: 人物设定辅助补齐强刷后仍 502 → 二次修复上线
- 用户反馈强制刷新后点击“辅助补齐”仍返回 502。
- 复查线上真实失败日志确认：页面弱上下文请求只有人物约束（如女性、17-25、学生），接口仍走 `luxury_ad.scene_config` 阶段模型，模型输出英文推理和空 schema；改用默认结构化文本模型后还会在弱上下文下改写身份为无关职业。
- 二次修复：人物补齐不再走场景配置模型；提示词明确为现代商业广告拟真演员表单补齐；已有性别、年龄、地域、身份/职业作为硬性锁定字段；服务端回写锁定字段，防止模型改掉用户选择。
- 生产探针用“无广告需求 + 女性 + 17-25 + 中国/东亚面孔 + 学生”验证返回 200，`roleName` 保持“学生”，补齐内容保持校园/学生语境。

## [16:18] 优化: 刷新后 avatar 旧任务地址 404 → 已优化上线
- 用户询问刷新界面后控制台出现多条 `/api/avatar/tasks/{id}` 404 的原因，以及拟真演员生成写入位置。
- 排查确认截图中的任务 ID 已不在生产 `avatar_tasks` 中；刷新时前端从本地缓存恢复过期任务/旧裸视频地址，导致浏览器请求后端不存在的裸任务地址。
- 优化：后端新增 `/api/avatar/tasks/:id` 旧地址兼容；存在任务接口访问返回详情、媒体访问跳转 `/stream`，不存在的媒体请求返回 204；前端恢复任务时清理服务器已不存在的旧任务，并把裸 `/api/avatar/tasks/:id` 视频地址规范为 `/stream`。
- 验证：不存在旧任务返回 204；存在任务 JSON 返回 200，媒体请求 302 到 `/stream`；生产健康检查 ok。

## [16:26] Bug: 拟真演员生成中字段可编辑与结果误报过期 → 已修复上线
- 用户反馈生成拟真演员过程中人物设定仍可编辑，并出现“人物演员包结果还未产生或已过期”错误。
- 根因：前端只禁用了生成按钮，没有把人物来源字段和同组动作按钮绑定到演员包生成中的 active 状态；后端人物演员包异步结果只保存在进程内 Map，PM2 reload/进程切换会丢失 running 状态，前端 accepted 后等待窗口不足，容易把“还没写回/进程已切换”误判为过期。
- 修复：生成中锁定人物数量、性别、年龄、地域、身份、姓名、外貌、穿着、发型、禁止项和同组动作按钮；前端 accepted 后最多等待 12 分钟，并给 missing 状态 90 秒宽限；后端将 request_key 的 running/done/error 状态持久化到 SQLite `app_kv`，查询时先读内存再读持久状态。若服务重启导致后台任务中断，返回明确的中断错误，不再统一说过期。
- 已同步生产并 reload PM2；生产本机和公网 `/api/health` 均返回 200。

## [16:59] 优化: 拟真演员按模型通道应用生成策略 → 已上线
- 用户确认可以按模型通道建立不同的人物生成规则，要求不能兜底、不能乱改、不能影响现有流程。
- 实现：新增人物演员包模型策略层，`deyunai/gpt-image-2` 走 `deyunai_gpt_image2_person_sheet_strict_v1` 严格提交审核策略；该策略只收敛提交给图片编辑通道的 prompt 和参考图数量，不修改用户保存的人物设定、不伪造成功、不跳过 QA。
- 规则：`gpt-image-2` 使用中性商业 casting sheet 表达、单张 clean reference、审核友好措辞；如果提交审核拒绝，只用同一模型和同一参考图再试一次最小审核 prompt，并在完整回执记录 policy/retry/audit 信息。其他模型仍走通用策略，后续新增模型可单独配置自己的 policy。
- 已同步生产 `src/routes/digitalHuman.js` 并 reload PM2；生产本机与公网 `/api/health` 均返回 200。
## [17:51] 用户需求
- 剧情广告拟真演员包生成体验继续优化：进度卡需要动态动画，普通用户不能看到 QA、actor_id、供应商链路等技术提示；生成结果必须匹配当前人物设定，否则不能通过；演员包需要补齐背面视图，总共四视图。

## [17:51] Bug: 拟真演员包三视图与设定不匹配 → 已修复上线
- 根因：后端演员包只生成正面、侧面/半侧、动作 3 个视图；QA 只校验构图和跨图一致性，未把当前人物设定中的地域/种族、身份、服装、发型妆造、禁止项作为动态硬校验。
- 修复：新增正面、侧面/半侧、背面、动作四视图生成；每张候选图新增动态人物设定匹配 QA，不符合当前字段的候选图不会入库。
- 前端：生成进度增加动画和长耗时柔性推进；普通用户只看到可执行提示，完整错误回执、失败候选图和 actor_id 等诊断信息仅内部调试权限可见。

## [20:33] 运维 / Git 同步与三方核查
- 按用户要求启动本地 VIDO 开发服务并从 `origin/master` 快进同步到最新提交 `5810aaa`，同步后重启本地 3007 服务，`/api/health` 返回 200。
- 核查结果：本地 `master`、Gitee `origin/master`、生产服务器 `/opt/vido/app` 均为 `5810aaa`，本地和服务器 tracked 工作区干净；GitHub remote 仍停在 `a1d2ad9`。
- 生产 PM2 `vido` online，生产本机和公网健康接口返回 200；数据库 root 与业务用户连通验证通过。未记录任何服务器或数据库凭证。

## [20:41] Bug: 拟真演员包人物设定 QA 误杀 → 已修复本地
- 用户确认后续只同步 Gitee，不再检查 GitHub 一致性。
- 截图问题定位：候选图已生成，但新增职业、外貌、服装、发型等字段后触发人物设定 QA；QA 的 malformed JSON 重试仍使用通用分镜 schema，且主观字段容易被当作硬失败，导致候选图被拒绝入库。
- 修复：人物设定 QA 使用专用 JSON 重试 schema；职业、外貌、地域等只在明显视觉冲突时失败，不因中性定妆照无法证明而误杀；生成侧补齐读取新字段进入角色提示。未绕过 QA，仍拒绝明确不匹配候选图。

## [21:18] Bug: 拟真演员生成人物设定 QA 再次失败 → 已定位生成端约束丢失
- 用户要求接入 ljg 工作流能力，但不能影响现有功能；同时继续排查拟真演员生成失败，禁止兜底、硬编码、绕过 QA。
- 复盘线上成功与失败样本：之前能成功的样本发生在人物设定 spec QA 生效前；当前失败发生在 spec QA 生效后，候选图清晰但没有满足当前“地域/种族、外貌、服装、发型、身份”等锁定设定。
- 根因：`gpt-image-2` 人物包策略会压缩生成 prompt，核心人物设定字段在长 prompt 后段容易被截断，模型仍按旧的泛化白衣牛仔裤演员出图；QA 拒绝是正确行为，不应绕过。
- 方案：在人物包模型策略中抽取并前置锁定人物设定，让生成端先收到约束，再由现有 QA 继续拦截不匹配候选图。
- ljg 接入方式：作为项目自有知识库 seed 引入视频、小说、图文工作流方法，不调用外部 skill 代码，不替换现有模型路由，不影响拟真演员包生成链路。

## [21:47] Bug: 拟真演员包返回 CreditInsufficient → 已定位为模型通道点数不足
- 用户提供 `/api/dh/luxury-ad/person-sheet` 完整回执，供应商原文为 `deyunai/gpt-image-2 code=400 reason=CreditInsufficient message=insufficient credits`。
- 排查确认：请求已经进入 `deyunai/gpt-image-2` 图片生成接口，不是模型未配置、不是人物设定 QA、不是 prompt 约束修复导致；失败发生在生成前，未产生候选图。
- 发现展示分类问题：后端顶层 code 仍归为 `LUXURY_PERSON_SHEET_NO_MODEL_OUTPUT`，前端额度识别未覆盖 `CreditInsufficient/insufficient credits`，容易让页面第一眼误解为普通无输出。
- 修复方向：仅修正错误分类和提示识别，归为 `PROVIDER_LIMIT_EXCEEDED`/429；不切换模型、不兜底、不绕过 QA、不写入失败结果。

## [22:16] 用户需求: AI 漫剧首页入口与任务中心整理
- 用户要求去掉 AI 漫剧页面里的设置和回收站入口，新增任务中心，用于查看全部、进行中、已完成漫剧内容。
- 同时要求减少页面上重复按钮，尤其是多处创建、状态胶囊和分享等无效/重复操作。
- 本次仅调整 AI 漫剧前端页面结构，不改后端接口、项目数据、生成链路或成片流程。
