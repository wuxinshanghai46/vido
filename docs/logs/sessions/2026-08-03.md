# VIDO 会话日志 - 2026-08-03

> 自动生成 · 项目助理维护 · 一天一文件

## 当日概览
完成场景动态拍摄细则、重生成进度、商品主体参考生成与全局图片灯箱的根因修复，并完成生产发布和只读核对。

## 事件流水

## [00:12] 用户反馈与交付
- 用户反馈场景缺少逐镜拍摄路线与动态细则，文字分镜重新生成只有提交提示、走动预案过长、已有场景仍显示生成、商品主体不能上传或生成，以及人物、场景和线稿大图打开慢且无法可靠切换。
- 根因确认：灯箱每次等待原图后才更新界面；异步提交后立即全量刷新重置了轮询；场景只有机位合同、没有结构化动态拍摄投影；商品服务把非独立商品生成全部拒绝；场景按钮没有读取既有资产状态。
- 已完成逐镜路线/器材/速度/时长/主体动作/焦点/起止状态/连续性细则、默认折叠预案、提交即显示 0% 进度、商品上传与主体参考生成、已有场景重新生成语义及缩略图即时灯箱切换。
- 本地浏览器验证场景灯箱 1/4 → 2/4、图片地址和描述同步，动态拍摄细则默认折叠且包含 4 段；控制台错误和警告均为 0。
- 生产发布后当前任务无活动生成、无错误；4 个文字分镜和 4 张线稿保留，历史 `camera_plan` 为空时由已验证机位生成可编辑建议，不自动覆盖用户数据或触发付费模型。

## [01:23] 用户需求与交付
- 人物正式生成入口改为“用户明确服装、鞋履、配饰优先，缺失项自动补齐”；仅否定某一种配饰不会误判为全部配饰已完整，明确无任何配饰则原样保留。
- 场景正式生成入口采用同一权威规则，生成前补齐布局、材质、光线、互动锚点、故事状态和行动路线，模型补齐失败时在图片生成前阻断。
- 完成周五至今 96 个代码与脚本文件的集中提交和生产发布；本地平台总回归、真实浏览器验收、生产定向回归、PM2、健康、SQLite 与任务只读核对均完成。
- 本轮测试与发布未触发真实图片或视频模型，既有任务和历史资产未被重写。

## [10:13] 用户需求 / 公司电脑同步与三方核对
- 本地分支以 fast-forward 从 `f869141` 同步到 `a1c3337`，与 Gitee `origin/codex/story-ad-v3-upgrade` 为 `ahead/behind 0/0`；原有删除项和未跟踪研究文档均保留，未覆盖。
- 外部交接 MD 与仓库版本逐行内容一致，仅 Windows/Linux 换行符不同；仓库版本已由远端同步到本地。
- 生产最终发布清单 96 个文件、周末提交涉及的全部 110 个代码/脚本文件均与本地内容一致，统一换行后差异 0；生产历史 detached HEAD 不作为运行文件权威依据。
- 生产 PM2 `vido` online，内外网健康 HTTP 200，SQLite `quick_check=ok`，活动生成任务 0；本轮仅做只读核对，未触发模型、媒体调用或业务写入。

## [11:22] 用户反馈与本地修复验证
- 用户反馈人物年龄显示内部英文占位值、年龄字段重复、完整人物档案重生成含义不清，以及所有模块灯箱字幕切换但图片不切换。
- 根因确认：年龄内部字段和正文重复投影；完整人物重生成实际已完成但文案只强调服饰配饰；全局媒体优化器缓存灯箱第一张图地址并覆盖后续动态切换。
- 本地已合并年龄到外貌正文、过滤内部年龄占位词、把入口与确认语义改为“重生成完整人物档案”，并锁定共享灯箱动态主图，确保所有同类模块真实切换图片。
- 定向回归、架构边界、全平台回归和真实浏览器核对通过；本轮未发布生产、未调用付费模型、未覆盖历史资产。

## [11:40] 决策与生产验收
- 用户明确授权发布生产，要求同步清理旧运行代码，避免浏览器继续调用旧版本。
- 首次发布因生产发布清单漏带两份新回归脚本而自动回滚；补齐清单并推送后第二次发布成功，运行代码和验证代码同步更新。
- 生产仅保留静态版本 `20260803-person-age-lightbox-r33`，旧 `r30/r31/r32` 命中 0；`public/story-ad` 文件集合与本地 21/21 一致，无孤儿旧文件。
- 线上真实浏览器确认年龄正文、完整人物重生成文案和正面到侧面的真实图片切换全部生效；浏览器错误/警告 0。

## [21:14] 用户需求 / 家中电脑同步与三方核对
- 本地目标分支从 `a1c3337` 安全 fast-forward 到 Gitee 最新 `03db17c`，`origin` 与 `gitee` 均为 ahead/behind 0/0；既有 5 个未跟踪文档保留，未覆盖。
- 本地 3007 服务已按新代码重启并返回 HTTP 200；生产 SSH、PM2、内外网健康、SQLite `quick_check` 和活动任务只读核对正常，未部署、未重启生产、未写业务数据或触发模型调用。
- 生产发布清单 73 个运行文件与本地统一换行后 SHA-256 为 73/73 一致；Git HEAD 相对运行代码提交 `9ac2f36` 仅新增 2 份文档，运行代码无差异。
- 完整平台回归除最终 V6 体积门禁外均通过；门禁因本机全局 `core.autocrlf=true` 将 Git 权威 314,621 bytes 膨胀为工作树 319,306 bytes 而失败，代码内容仍低于 310 KiB 上限。GitHub 镜像仍落后 14 个提交，本轮未获授权推送。

## [21:41] 用户需求 / 人物真实感与 3D 导演台竞品核对
- 用户授权参考微信文章与 LibLib/LibTV 导演台项目，要求结合既有差距分析，查明人物、场景真实感不足和主卡片无法放大查看的原因，并形成一次性整体优化方案。
- 核对确认：现有人物档案先生成 4 张低分辨率合集再本地切成 20 个原子素材；关键帧参考仍按固定优先级截取最多 4 张；人物与视频质检缺少皮肤、毛发、脸部锐度、光照融合和跨帧漂移等真实感门禁。
- 当前场景世界主要是资产展示、二维代理、机位清单和人物分配；其覆盖数据未编译为包含角色变换、镜头 FOV、机位截图和运动轨迹的镜头参考包进入关键帧生成。竞品 3D 导演台主要提升空间关系、构图与运镜一致性，不能替代人物真实感链路。
- 主资产卡没有启用或绑定全局灯箱，场景主卡使用背景图且点击进入场景；抽屉虽有灯箱，但仅适配屏幕，没有真实缩放和平移。现有测试只覆盖抽屉/辅助页面，因此遗漏了用户实际入口。
- 本轮仅做只读研究与本地回归验证，未修改业务代码、未调用付费模型、未写业务数据或部署生产。

## [21:50] 决策 / 真实感升级的性能、冗余与新版本门禁
- 用户要求后续详细方案必须保留当天刚补齐的人物服装/鞋履/饰品/年龄、场景布局/材质/光线/交互/行动路线和异步进度等权威链路，禁止另起重复事实源。
- 方案确定采用“现有权威数据 + DirectorScene 引用层 + ShotReferencePack 编译层”，导演状态只保存实体 ID、revision、变换、机位和轨迹，不复制人物/场景正文；历史资产可查看但不得被新生成链路静默选用。
- 当前新版工作区 22 个文件约 412 KB（gzip 115 KB），旧公开脚本树 45 个文件约 522 KB（gzip 159 KB）；旧 UI 主脚本已返回 410 且旧入口跳转新版，但后续仍需把死运行代码移出公开目录，仅保留轻量重定向兼容层。
- 当前已具备 build/contract 响应头、旧客户端写入 426、HTML 与剧情广告 ESM no-store、内容 revision 冲突保护；后续升级为单一发布清单、哈希静态资源、启动前版本握手、长开标签页心跳与原子部署，兼顾最新版本强制命中和缓存加载速度。
- 本轮只完成只读架构与发布方案审计；入口关闭、内容版本、当前输入权威、V6 UI 和 120 镜轮询性能回归通过，真实模型调用 0，未修改业务代码或部署生产。

## [23:15] 用户需求 / 完整开发与发布前验收
- 用户确认按通用升级方案完整开发，要求兼容所有行业和场景、保留当天新增的人物与场景细节、控制代码冗余与加载速度，并强制用户命中新版本；网络恢复后要求继续完成。
- 已完成原生脸部/全身人物主图、真实感与跨帧 QA、DirectorScene 3D 导演台、逐镜 ShotReferencePack、场景/人物统一大图缩放平移、默认 1080p 成片与发布版本门禁；所有新增结构只引用权威实体 ID 和 revision，不写死行业或场景。
- 根因验证发现 Three.js 模块依赖的核心分包遗漏，以及两处旧回归仍写死上一版 build ID/四图人物契约；已修正构建清单和测试权威来源，未通过降低业务质量标准绕过失败。
- 本地静态检查 47 个 JS 文件通过；V6、人物档案、V2/V3 全链路、平台加载/能力和视频画布回归全部通过；真实浏览器验证 3D 画布动态加载成功、媒体缩放 100% → 125%，真实付费模型调用 0。
- 发布前生产只读核对：PM2 online、内外网健康正常、SQLite quick_check=ok、活动生成任务 0；待 Git 提交推送与原子生产发布后再形成最终可测试结论。

## [23:23] 交付 / 生产发布与三方核对
- 功能提交 `c5431e5` 已推送到 Gitee `origin` 与 GitHub 镜像，两个远端均与本地 ahead/behind 0/0；用户原有 5 个未跟踪研究文档保持未改动、未纳入提交。
- 生产原子发布 63 个代码/静态/验证文件，SHA-256 核对 63/63 一致；服务器隔离目录执行 `platform:upgrade:test` 退出码 0，真实模型调用 0。
- 发布后 PM2 `vido` online，内网与公网健康均 ok，发布版本为 `20260803-photoreal-director-v8 / director-scene-v1`，SQLite quick_check=ok，活动生成任务 0。
- 外网独立核对确认版本化资源 immutable、未版本化资源 no-store、旧客户端写入返回 426；生产备份位于 `/opt/vido/backups/story-ad-photoreal-director-v8-20260803151728`。
- 本轮未执行真实人物/场景/视频生成，避免重复计费与覆盖历史数据；开发、发布和只读状态门禁全部满足，可以进入用户验收测试。
