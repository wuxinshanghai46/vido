# VIDO 会话日志 - 2026-08-07

> 自动生成 · 项目助理维护 · 一天一文件

## 当日概览

完成 v71 本地、Origin/Gitee、生产服务器三方一致性复核，并生成公司续接交接文件。

## 事件流水

## [00:15] 用户需求：三方核对并生成公司续接交接
- 用户要求确认服务器、Git、本地代码一致后生成交接 MD，便于明天到公司拉取并继续优化。
- 本轮只做只读核对和文档交接，不修改业务代码、不触发模型或媒体生成。

## [00:25] 完成：v71 三方一致性核对
- 本地运行清单 301/301、Git 发布文件 302/302、生产发布文件 302/302 哈希差异均为 0；本地与生产版本、合同和运行哈希一致。
- Origin/Gitee 与核对前 HEAD ahead/behind 0/0；GitHub 镜像落后 32 个提交，不作为权威拉取源。
- 生产 PM2、内外网健康、数据库、SQLite 和活动任务只读核对通过；活动任务 0，本轮没有模型、媒体调用或业务写入。

## [21:00] 运维连接：启动本地项目并连接生产服务器
- 用户要求启动项目并连接生产主机 `43.98.167.151`。
- 本地开发服务已在 3007 端口启动，`/api/health` 返回 HTTP 200。
- 使用 SSH 密钥经 2222 端口成功连接生产；PM2 `vido` 为 online，内网与官方 HTTPS 公网健康接口均返回 HTTP 200。
- 生产 SQLite 以 URI 只读模式执行 `PRAGMA quick_check` 返回 `ok`，活动生成任务为 0；未部署、未重启、未写入业务数据，也未触发模型或媒体调用。

## [21:05] 竞品流程分析：Codex + Skill 视频创作
- 用户授权浏览器分析抖音视频，询问 VIDO 是否已具备故事、脚本、分镜、视频提示词的 Skill 化能力。
- 页面实证显示视频主张四层降维：故事 → 脚本 → 分镜 → 视频提示词；人负责故事判断和分镜审核，Skill 负责把分镜翻译为光影、机位、动作和参数，Codex 负责执行。
- 代码核对确认 VIDO 已具备更完整的内部业务链：剧情蓝图、文字/线稿分镜、逐镜导演设计、关键帧合同、视频提示词编译、生成与 QA，并有人工确认门禁及任务级知识策略快照。
- 当前能力属于平台内置 Agent、服务和知识策略，尚不是可由 Codex 标准加载/复用的 `SKILL.md` 包；后台 Skill 页面目前只有元数据增删，没有运行时加载与执行链。建议后续封装为可版本化的 VIDO AI 导演 Skill，而不是重复建设生成逻辑。

## [21:06] 方案：VIDO 视频创作 Skill 封装
- 用户询问具体封装方式。
- 建议采用两层架构：Codex Skill 只保存触发规则、阶段流程和安全门禁；VIDO 适配层复用现有业务服务，提供稳定、幂等、可恢复的动作接口。
- Skill 应在剧情、分镜、镜头设计和付费生成前停止并等待人工确认；费用授权绑定项目、内容版本、生成范围、成本上限和幂等键，避免旧确认复用与重复付费。
- 推荐先交付本地 Skill + Node 客户端 MVP，再把后台 Skill 管理从元数据目录升级为版本、启停、动作与执行记录管理；本轮未修改代码。

## [21:07] 决策建议：Skill 归属与迁移
- 用户询问 Skill 是平台能力还是本地能力，以及迁移服务器时是否会一并迁移。
- 建议将 VIDO Skill 源码作为平台资产纳入 Git 和发布清单，本地 `$CODEX_HOME/skills` 仅保留可重装的运行副本。
- 平台迁移时迁移 Skill 源码、VIDO 动作适配层、知识策略与版本清单；API 凭证和密钥通过目标环境独立配置，不进入 Skill 或 Git。
- 若 Codex 只在用户电脑运行，生产服务器无需安装 Skill；若未来服务器运行 Codex Agent，则部署流程按锁定版本自动安装同一 Skill。

## [21:09] 架构澄清：服务器无需安装 Codex
- 用户指出若 Skill 只有 Codex 安装端生效，服务器端如何获得同等能力。
- 结论调整为双实现面：VIDO 服务端原生工作流是所有网页用户共享的权威能力；Codex Skill 只是调用同一工作流的可选客户端，不承担核心业务执行。
- 服务器部署 `VideoDirectorWorkflowService`、动作 API、状态机、审批与费用门禁即可，无需安装 Codex。只有需要服务器自主自然语言 Agent 时，才另行接入模型 API/Agent 运行时。
## [21:14] 竞品核对与架构纠正：平台原生 Skill
- 用户明确平台用户应直接输入内容并使用 Skill，核心能力不应依赖 Codex 的安装位置。
- 核对 LibTV 官方 Skill 中心、CLI 页面及公开 `libtv-skill` 后确认：其平台内有 100+ AI 导演 Skill；公开 CLI Skill 只是外部 Agent 的传话与结果下载适配层，需求拆解、分镜、提示词、模型选择和工作流编排由 LibTV 服务端 Agent 完成。
- VIDO 应建设服务端原生 Skill 实体、目录、路由器、运行器与执行记录；网页用户直接选择或自动匹配 Skill，外部 Codex/OpenClaw 连接器仅作为可选入口。
- 竞品公开问题显示需重点补足执行计划与解析后提示词可见、dry-run、幂等与成本上限、项目/会话管理及版本化追踪；本轮仅做竞品研究和架构澄清，未修改业务代码。
## [21:18] 决策建议：保留平台链路，实施轻量原生 Skill 层
- 用户询问是否确有必要封装 Skill，还是继续使用平台现有能力即可。
- 建议不做 Codex 本地 Skill，也不重写现有生成链路；短期保持现有流程运行，只新增服务端轻量 Skill 定义、版本、路由和执行快照，将现有 Agent/服务编排配置化。
- 若近期目标仅为内部固定流程，可暂缓 Skill 市场和用户发布功能；若目标包含多类型模板复用、自动选片、用户自定义或生态发布，则原生 Skill 产品层有必要建设。

## [22:02] 用户需求与完成：平台 Skill、AI 辅写、3D/360、线稿和删除闭环
- 用户要求封装后可直接测试，并补齐人物/场景 AI 帮写、3D、360/3D AI 规划、线稿与故事衔接、广告/剧情分流、表单对齐和项目永久删除。
- 根因确认：人物/场景辅助后端已存在但新版资产中心漏接；360 由指纹与数字计划双重校验造成误拦；3D 仅有真实结构导演预演而未配置 6DoF 重建供应商；线稿缺少故事与前后镜连续性上下文；删除后端已有记录删除但前端漏入口且未清理专属文件。
- 已实现服务端内置内容 Skill 快照（广告/纯剧情）、任务级知识策略接入人物/场景/空间规划辅助、结构化 3D 导演预演入口、360 最新指纹确认、故事连续性线稿合同、危险确认弹窗和安全文件清理。
- 本地定向测试、V6 完整回归、V3 全链路回归和浏览器验收均通过；浏览器验证设置控件高度均为 42px、同排顶边一致，删除危险弹窗样式正确。未在浏览器触发真实人物/场景/360模型调用，避免重复付费。

## [22:10] 部署完成：v78 生产验收
- 发布前平台全量升级回归通过，真实模型调用 0；活动任务发布前后均为 0。
- 生产 308 个发布文件哈希差异 0，PM2 新进程 25617；内外网健康、数据库和 SQLite quick_check 均为 ok。
- 公网独立复核版本为 `20260807-platform-skills-ai-v78`、合同 `platform-skills-v1`、运行哈希与本地发布清单一致；静态清单包含 42 个前端文件。

## [22:24] 复核：允许用户重新测试 v78
- 用户确认未配置 6DoF 供应商仅影响真实空间平移、碰撞与遮挡，不影响分镜、人物/场景锁定、结构化 3D 导演预演、关键帧和视频主链路。
- 重新核对生产版本、PM2、内外网健康、数据库、SQLite 和活动任务：v78 进程 25617 在线，健康与数据库为 ok，SQLite quick_check 为 ok，活动生成任务为 0。
- 本轮仅做只读状态核对，未修改业务数据、未触发模型或媒体生成；用户可从新任务开始测试，真实 6DoF 仍保持不可用。

## [23:39] Bug：同一人物的古代/现代造型被合并 → 多造型结构化闭环
- 用户反馈任务“千年回忆”的林静应有古代、现代两个造型，但人物详情只显示一个混合服装字段。
- 根因确认在最上游人物资料合同：人物只有标量 `wardrobeText`，资产计划、生成规格、检查点、分镜引用和前端均沿用该标量，导致同一身份的跨场景造型被压平，而不是单纯的展示问题。
- 已升级为同一人物下的场景绑定 `look_profiles`，并贯通 AI 辅写、资产计划、逐造型补全/生图、检查点指纹、分镜 `look_id`、参考图选择和前端编辑；缺少场景绑定的多造型会在付费生成前阻断。
- 已将生产旧任务精确迁移为“古代造型 + 现代造型”，模型调用总数保持 27 不变，旧失败检查点保留审计但因新指纹不会被续用；浏览器只读验收确认两张造型卡和两个独立场景/服装字段均正确。

## [23:52] 故障排查：重新生成人物仍失败
- 用户提供支持编号 `a0750221-f416-4af1-9907-329309c03332`，要求判断是模型问题还是平台问题。
- 生产只读证据确认终止错误为 `PERSON_WARDROBE_AUTO_COMPLETION_INCOMPLETE`：服装完整性校验词表无法识别已经明确存在的“宋式长衫”，本地等价复现把古代造型误报为缺少 `garment` 和 `lower`，现代造型则通过；最早错误源在平台服装语义校验，不是图片模型。
- 本轮重试还出现 3 次文本模型业务语义校验失败，随后 1 次文本模型成功，但最终仍被平台词表误判拦截；v81 之后共新增 4 次文本调用，没有新增图片模型调用。
- 历史 `gpt-image-2` 仍有两个 `PROVIDER_5XX_AMBIGUOUS` 计费未知单元且一次性重试授权尚未消耗；任务当前无活动生成。排查阶段未修改代码或生产数据，并明确在根因修复前不要继续点击生成，避免授权被消耗和潜在重复计费。

## [00:00] 每日学习事件（自动 00:00）
- KB 新增: 0 条
- Agent digest: 30 份
- 详情: docs/learning/2026-08-07/_summary.md
