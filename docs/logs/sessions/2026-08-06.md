# VIDO 会话日志 - 2026-08-06

> 自动生成 · 项目助理维护 · 一天一文件

## 当日概览
完成计费未知资产的一次性用户授权实测；配饰供应商再次返回相同 500，系统重新锁定。同时闭环场景合同自动补齐前被提前拦截的问题。

## 事件流水

## [00:18] 用户授权实测与再次失败
- 用户明确接受一次可能重复计费后，生产任务提交了一个补生成 generation；6 项人物核心资产继续复用。
- 配饰唯一授权被消费，供应商再次返回 `500/image2O100IFR`；模型调用总数由 8 增至 9，任务于 00:10 停止并重新进入计费未知锁定，没有第三次提交。
- 场景分支在模型调用前因首个空间缺少 `materialLightText` 失败，确认是解析层先于既有自动补齐器拒绝不完整逐空间合同。
- 用户当前不可继续点击配饰补生成；需供应商账单/服务恢复或切换经验证的图片供应通道。

## [00:24] v40 生产完成
- v40 已通过服务器完整回归并部署；场景合同缺字段会进入自动补齐，补齐完成后才可调用图片。
- 旧任务保持 failed、活动 generation 为 0、模型调用总数为 9；配饰再次未知计费已锁定，部署没有续跑。
- 用户可刷新后通过“查看 / 单独生成场景”逐场景继续；不可再次点击配饰费用风险入口。

## [00:38] 公司续测交接
- 用户要求核对本地、Git、生产三方一致后生成交接文档，并说明本轮主要优化。
- 本地与三套远端代码基线一致；生产 144/144 运行文件哈希一致，PM2、内外网健康、数据库与 SQLite 均正常，活动任务为 0。
- 交接明确区分代码一致与外部图片通道可用性：供应商 500 和未知计费仍未解除，明天应先查账单/通道，再做单项低成本测试，不得直接重复联合生成。
- 交接提交已同步 `origin/gitee`；GitHub 443 连续不可达，仅交接纯文档镜像待网络恢复后补推，v40 运行代码不受影响。

## [09:44] 公司电脑同步与三方复核
- 用户要求启动项目、从 Git 拉取最新代码，并复核本地、主远端、生产服务器与 v40 交接 MD 是否完整同步。
- 在保留既有删除项、历史日志追加和未跟踪研究文档的前提下，本地从 `71132ed1` fast-forward 到 `05a1bac9`；本地与 `origin/codex/story-ad-v3-upgrade` ahead/behind 为 `0/0`。
- 拉取后按新 `.gitattributes` 将 7 个旧工作区 CRLF 发布文件恢复为清单要求的 LF；发布完整性门禁通过，本地 v40 服务在 3007 返回 HTTP 200。
- 本地与生产 144 个发布运行文件按统一 LF 核对差异 0；生产 PM2 online，内外网 health、数据库和 SQLite 正常，活动任务 0。
- 旧失败任务仍为 `failed / visual_assets_failed`，无活动 generation，6/19 资产保留，模型调用总数 9；未触发新模型、媒体调用或业务写入。
- 外部交接 MD 与仓库跟踪文件均为 184 行，规范化 SHA-256 完全一致；本地完整平台回归退出码 0，用时 298.1 秒。

## [10:01] 用户反馈与 3D 场景入口诊断
- 用户反馈此前存在的 3D 场景功能在要求移动到场景资产之后看似消失，询问是否必须等待全部场景生成。
- 代码核对确认功能未删除；当前 SceneWorld 只投影具有真实视觉资产的场景，0 个就绪时完整工作区会替换为“等待场景视觉”锁定卡，且锁定卡未直接显示“3D场景”字样，容易造成入口消失的认知。
- 门禁不要求全部场景完成：任意 1 个场景具备视觉资产即可开放该场景的 SceneWorld，并显示部分完成数量；未完成场景继续排除。
- 当前旧任务场景为 0/11，因此不会显示每个场景内的“规划360 / 3D”按钮；360 全景和真实 6DoF 空间仍分别需要合格全景资产及几何/navmesh，不会由普通场景图自动冒充。

## [10:11] 用户反馈与重新生成按钮命名修正
- 用户指出计费未知状态下“接受费用风险并继续缺失项”的按钮名容易误解，应明确命名为“重新生成”。
- 已将按钮改为“重新生成”，风险事实、一次性授权与再次未知计费重新锁定仍保留在二次确认流程；相关提示同步指向新按钮名称。
- 发布版本升级到 v41；定向、静态、边界、发布完整性和完整平台回归全部通过，本地 3007 服务加载 v41，浏览器实际入口脚本和样式缓存版本均为 v41。
- 本轮未点击重新生成，真实模型与媒体调用为 0；尚未提交、推送或部署生产。

## [10:22] 生产图片超时与审核规则核对
- 按支持编号只读定位到任务：本轮鞋履细节请求在提交供应商后等待 300362ms 超时，未收到 HTTP 状态、供应商错误码、原因、请求 ID 或任务 ID，计费状态保持 unknown；未自动重试。
- 相邻腕部配饰请求使用同一 deyunai/gpt-image-2 成功并返回任务 ID；鞋履提示词强调原创、无文字/标识/水印，引用的是任务内 AI 人物资产。审核指南中的敏感类别没有直接命中证据，内容审核只能作为外部可能性，不能作为结论。
- 场景错误与图片审核无关：生产 scene_config 中 materialLightText 实际完整；工作区项目投影把该组合字段转换成空的 materials/light，合并生成再提交这个不完整 scene_spec 并覆盖服务端完整空间规格，因而在模型调用前失败。
- 当前任务无活动 generation，PM2 online、内网健康 HTTP 200、SQLite quick_check ok；没有触发新模型调用或业务写入。

## [11:16] 用户反馈、通用场景修复与配件知识调研
- 用户指出场景规划入口在未操作时不应呈现默认主按钮状态，并再次确认费用风险入口应命名为“重新生成”；两项前端语义已统一并发布。
- 场景规格问题按通用链路修复：项目投影保留权威场景字段，旧前端提交缺字段时按 space_id 从服务端权威规格补齐；11 个场景回归覆盖同名字段、旧客户端兼容和跨场景隔离。
- 用户进一步询问配件卡住问题，并授权阅读一篇微信文章、结合平台能力提炼入库；内置浏览器因站点安全策略阻止访问，未绕过限制，等待用户提供正文、PDF 或长截图。
- 知识库检索确认目前只有人物一致性和配饰描述类泛化知识，没有配件失败降级、成功资产复用和避免重复付费的专门条目；代码中本地配件裁切能力尚未接入当前正常人物生成链路，当前仍逐件调用图片模型。

## [11:40] 文章截图审阅与知识库补充
- 用户提供 16 张文章截图，核心方法是用固定版式组织主身份锚点、标准三视图、六种表情、服装配饰拆解和材质细节，并要求同版所有视图共享五官、发型、服装、配饰与身材比例。
- 平台对照确认现有人物档案已覆盖四个身体视图、四个身份细节、六表情、六动作以及服装/配件细节，能力范围高于文章示例；采纳的是结构化视觉合同与配件清单，不采纳一次模型调用生成整张含文字拼版的方式。
- 用户追问知识规则是否影响当前人物生成；已明确本轮只新增知识种子、索引、测试和发布清单，没有修改人物生成服务、提示词、模型参数、配件调用顺序或失败边界。
- 新增两条知识：人物资产卡视觉合同；配件 L1 复用、L2 本地裁切、L3 模型增强以及计费未知停止重试的证据降级协议。
- 本地与生产均已检索到两条知识；本地 3007 服务恢复并返回 HTTP 200。

## [11:52] 决策：知识库必须进入生成与质检闭环
- 用户明确要求知识库不能只作为 Agent 阅读材料，而要稳定提升人物、场景、关键帧和视频结果，并要求完整实施方案。
- 当前代码核对结论：剧情广告最终人物、场景和视频提示词由固定编译器与任务合同生成，知识库仅在通用 Agent/AI Team 上游注入，尚未绑定最终生成合同、指纹、供应商适配和视觉 QA。
- 后续方案必须采用可筛选、可版本化、可追踪、可回滚的知识规则编译，不允许把整篇知识或自由检索结果直接拼进付费模型提示词。

## [12:39] 用户需求：完整落地通用知识驱动生成方案
- 用户要求知识规则真正作用于人物、场景、关键帧和视频生成/质检，同时保持行业与场景无关，控制代码体积、查看性能与生成耗时，并完成全量测试后交由用户验收。
- 本轮采用任务级不可变策略快照、确定性本地编译、生成与 QA 指纹分离、局部失效和配件本地优先证据链；没有把文章示例或任何行业词写死到业务分支。
- 用户验收前不触发真实付费模型；本轮所有自动回归的真实模型调用为 0。

## [13:14] 完成：通用知识生成运行时已发布
- v48 已完成本地、Git、生产三方同步；人物、场景、关键帧和视频链路按任务快照消费规则，管理端可配置并追踪规则版本。
- 性能门禁确认首屏 11 请求、约 47 KB gzip；热编译 P95 低于 0.1ms，规则编译不调用模型；配件本地优先与局部失效减少重复付费和等待。
- 生产发布与独立复核均通过，现可由用户进行完整验收测试。

## [14:02] 反馈：增强按钮指向与执行状态
- 用户反馈当前按钮悬停指向不明显，希望参考此前版本的高对比颜色与清晰动作反馈；同时询问计费未知任务能否直接点击“重新生成”。
- 只读核对确认“重新生成”仅在上一轮供应商计费状态未知时出现，仍代表可能重复计费的一次性授权，不是普通下一步；当前不得直接点击。
- 交互统一增强为精确指针悬停上浮、薄荷色描边/文字、强阴影与外圈，按下回弹、键盘焦点、执行中及减少动态效果状态同步覆盖。
- 本地浏览器实际核对 v49 缓存版本和悬停计算样式；完整平台回归退出码 0，真实模型调用 0。

## [14:26] 反馈：计费未知任务需要可执行的安全重试路径
- 用户追问不能直接点击“重新生成”时应如何重新尝试；确认其诉求是继续当前人物与场景任务，而不是永久停止。
- 当前联合“重新生成”会先消费一次性重复计费授权，再继续配饰、宠物和全部缺失场景；成功的 6 项人物核心资产会复用，但供应商通道仍只有 `deyunai/gpt-image-2`，近期日志没有新的恢复成功证据。
- 当前可分离恢复：先从“查看 / 单独生成场景”选择一个场景做最小范围尝试，不会重提计费未知的配饰；人物配饰需先核对供应商账单或明确接受可能重复计费后再使用联合恢复。

## [14:42] 生产诊断：单项入口误锁、供应商再次 5xx 与成功资产不展示
- 支持编号 `6c35a6a0-9c88-40b8-84ce-6867f231e5c9` 对应任务已停止于 7/19；人物 6/8、场景 1/11，任务状态 failed，无后台继续调用。
- 生产证据确认 14:24–14:30 已写入人物身体、身份、表情、动作、脸部/身体母图和第一个场景的图集、四个派生视图及布局候选；文件未删除。
- 单人物“生成完整人物档案”被前端全局 `billingReviewRequired` 直接拦截，未发起模型调用；这是入口授权粒度错误，不是用户操作错误。
- 联合链路使用人物 `deferCommit` 与场景 `deferPublish`；后续人物服装细节和场景布局重试分别收到 `500/image2O100IFR` 后，正式 context 未提交，因此资产卡仍读取旧草稿，成功 checkpoint 不可见。
- 本轮供应商在同一任务内先返回 9 个成功任务 ID，随后两个无任务 ID 的不透明 5xx；确定事实是供应商 500，内容审核或服务故障仍无法从公开错误码判定。两个并发分支没有在首个计费未知后立即互相取消。

## [15:27] Bug：视觉资产计费未知恢复链路系统级修复
- 用户要求按已确认根因实施完整修复，并明确后续其他用户是否仍会遇到同类问题；修复定位为公共生成层和项目投影层，不绑定当前任务、用户、行业或场景。
- 同一任务的付费图片调用改为串行安全门：首个不透明 5xx/计费未知后，尚未提交的排队单元在本地停止；不同任务仍可并发，避免一个用户阻塞其他用户。
- 计费风险由页面级锁改为人物/场景具体 checkpoint 逐项核对；单人物、单场景和联合入口只授权自身范围，且每项授权仅可消费一次。
- 人物与场景成功 checkpoint 作为只读部分资产投影持续展示；人物单项重试会续用旧部分 checkpoint，不因新的点击请求键重复生成已成功核心图。
- 发布版本为 `20260806-partial-asset-recovery-v52`；本地浏览器确认加载 v52 且控制台无错误。所有自动测试使用替身或静态检查，真实模型调用 0，未修改生产任务数据。

## [15:46] 验证反馈：发现更深层场景投影边界
- v52 发布后对目标任务只读核对：任务和 23 条模型调用记录均未变化，人物局部成功结果已显示；场景因 checkpoint 聚合状态为 failed 仍被隐藏。
- 已撤回可测试结论并继续修复；展示规则改为读取每个视图的成功状态，不再用聚合失败状态抹掉已成功、已付费的视图。

## [16:06] 验证反馈：场景状态在工作台二次投影丢失
- 只读拆层探针确认生产 checkpoint 为 partial，主/反向/互动/细节 4 视图成功，布局失败；底层投影正确，但工作台场景卡丢失 partial 标记。
- 已补齐跨层字段并增加失败 checkpoint → 底层投影 → 工作台场景卡的回归，继续保持不可测试状态，等待新版本完整发布和同任务只读复核。

## [16:25] 验证反馈：新 checkpoint 被旧失效边界误拦截
- 生产数据确认当前场景规划和 checkpoint ID 完全一致，4 张视图成功；阻塞来自 `scene_assets` 的旧失效标记早于本轮 checkpoint，但公共任务视图没有区分先后时间。
- 修复改为时间线 + 当前规划 ID 双重判定，既不恢复失效旧资产，也不丢弃修订后新生成的成功资产；等待 v56 发布后再次对同一任务只读核对。

## [17:34] Bug：原始创意覆盖、故事误商品化、人物数量与场景漂移
- 用户反馈真人上传弹窗背景穿透导致无法输入；“现代和古代交替”只规划 1 人；“女孩在竹海漫步”被替换成书房；纯故事剧情被强制解释为商品广告。
- 根因确认：弹窗使用 4 个不存在的 CSS 变量；`brief_goal` 把模型结果整体覆盖原文；无明确商品时后端仍从任意广告文本推断商品；资产规划缺少跨时代独立人物与明确地点的结果门禁。
- 修复为原文不可变追加、纯故事/商业主体权威分类、现代/古代交替默认双人物（明确同一人物时除外）、明确地点生成前提示与生成后拒绝漂移；真人弹窗恢复实体背景、焦点、遮罩隔离、Esc 关闭和焦点还原。
- 浏览器无付费验收显示 2 个人物、1 个竹海场景、0 个商品，文本框可正常输入；完整 v3/v6 回归、发布完整性与体积边界通过，真实模型调用 0。

## [18:04] 决策：创建前显式选择广告或剧情
- 用户要求在内容目标前增加“广告 / 剧情”选择，让 AI 帮写、人物场景方案和后续脚本使用对应策略，不再仅靠关键词猜测。
- 页面未选择时阻止 AI 帮写、创建项目和创建人物场景方案；参考识别或旧数据不替用户默认选中；选择态使用高对比描边、底色、阴影和键盘焦点反馈。
- 显式选择记录为 `content_mode_source=user` 并保持最高优先级；旧版本自动推断值记录为 `inferred`，可被参考视频后来识别出的明确商品证据纠正。
- 浏览器实测未选择拦截、剧情选中、输入和保存回填均通过；保存请求为纯剧情、商品为空，测试项目已删除，真实模型调用 0。

## [18:23] 部署与只读验收
- v63 已发布生产；298/298 文件哈希一致，PM2、内外网健康、数据库与 SQLite quick_check 全部正常，活动生成任务为 0。
- 目标旧任务没有被改写或续跑；旧错误内容仍在，v63 因缺少用户选择来源而保持未选中并阻止继续，等待用户选择剧情并修正内容目标后再验收。
## [18:35] 用户需求：三方一致核对并生成回家续接交接
- 用户要求核对本地、Git、生产服务器代码，确认一致后生成并推送交接文件，便于回家拉取继续优化。
- 初检没有直接宣告一致：发现 v63 本地/生产与 Git 新克隆之间存在 146 个文本文件的换行符字节差异；已升级 v64 并按 LF 构建，待提交、部署及三方逐文件哈希复核完成后再生成最终交接。
- 保留工作树中用户原有的旧日志、删除标记和未跟踪研究文档，不纳入本次提交。
