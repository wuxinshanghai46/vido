# VIDO 剧情广告 v207 回家续接交接

> 更新时间：2026-08-12 18:22（Asia/Shanghai）
> 分支：`codex/story-ad-v3-upgrade`
> 生产：`43.98.167.151:4600`
> 本轮运行代码提交：`02a954413a12de79a341f51408c11c43ee6fd901`

## 1. 本轮完成内容

- 广告与剧情保留通用资产、场景、镜头、音频和发布底座，但内容类型、剧本规则、提示词包和质量门禁明确隔离；未确认内容类型时停止生成。
- 工作区调整为六步：目标与材料、资产中心、场景世界、剧情室、分镜设计、生成与成片。
- 人物与场景改为两个独立生产流程；人物按单人物任务生成，场景按单场景任务生成，避免一次补齐全部缺失内容导致整批失败。
- 人物支持多套造型、独立服饰/鞋履/配饰、年龄状态和跨造型身份连续性；人物卡显示单人物主图，详情保留完整档案和原子资产。
- 场景文字方案预分配地点、机位、人物出场、造型和地点沿革；“回到以前的竹林”按同一地点重建后的现代竹林处理，不再误投影成现代城市。
- Brief 正文保留换行与段落，刷新后使用统一 5000 字持久化、API 和展示合同，编辑框内部滚动。
- Image 2 的 500/计费未知保持停止自动切换与重复付费；用户端隐藏供应商原始错误、支持编号、模型名和内部计费说明。

## 2. 数据流变化

修改前：目标文本可能被压成单行或截断；广告/剧情依赖隐式推断；人物与场景批量补齐；旧七步导航与新六步页面并存。

修改后：用户明确选择内容类型 → 类型专属 Skill/提示词/产物门禁 → 人物档案与场景世界分别确认 → 第 4 步生成剧本 → 第 5 步分镜、线稿和镜头设计 → 第 6 步视频、配音、场景音效与成片。人物、造型、年龄状态和场景地点均保存稳定 ID 与版本血缘。

## 3. 关键提交

- `980000d`：六步工作流、人物演化/多造型、场景世界和广告/剧情隔离主体实现。
- `1542422`：前端与服务端模块边界拆分，满足发布体积门禁。
- `9a291e4`：旧测试夹具补充显式内容类型。
- `445b70c`：内容类型切换迁移、产物标记和严格门禁。
- `02a9544`：V6/历史兼容门禁统一到六步流程、人物/场景独立任务。

## 4. 本地、Git、生产一致性

| 对象 | 状态 | 依据 |
|---|---|---|
| 本地运行代码 → origin | 一致 | `HEAD=02a9544...`，ahead/behind `0/0`（写本文档前） |
| 本地不可变制品 → 生产 | 一致 | 663 个发布文件经发布器逐文件 SHA-256 审计；生产 current 指向相同 artifact |
| 生产构建 | 已上线 | build `20260812-ui-v207`，artifact `031bbd1517a5016f0d09821665891e1b4e521953517eb02bbd3f330398e909e3` |
| 运行合同 | 一致 | bundle `124c2e45dbee2823d271cfac0813f1b30de70f53cbb5e044bb34ab53a0fff4f1`，runtime hash `e9fac70369afd10cec3fe47eeebdd1c49c02ec8afee86ffe6671896fddba0d71` |

本文档提交后，Git 最新 HEAD 会只比生产运行代码多一个纯文档提交；生产运行文件仍与 `02a9544` 对应制品完全一致，无需为交接 MD 重发生产。

## 5. 已执行验证

- `npm run story-ad:v3:test`：通过；真实模型调用 0。
- `npm run story-ad:v6:test`：通过，包含参考同步、场景世界、导演画布、人物多造型、模式隔离、发布完整性与边界检查。
- `npm run story-ad:v197:test`：5 项全部通过，覆盖段落合同、内容域隔离、人物演化、六步工作流、场景音效和 Image 2 安全失败边界。
- 发布门禁：`LOCAL_IMMUTABLE_RELEASE_GATE=passed`；完整平台回归通过。
- 发布后公网 `/api/health`：HTTP 200 / `status=ok`。
- 发布后 `/api/story-ad/version`：build、artifact、bundle、runtime hash 与本地清单一致。
- PM2 `vido`：online，PID `2985`，restart `0`。
- SQLite `quick_check`：`ok`。
- 活动任务：`0`；active unknown billing：`0`；历史 unknown billing：`59`，发布前后未变化。
- 本轮测试和发布没有触发真实图片、视频或付费模型调用。

## 6. 未执行项与风险

- 未执行真实 Image 2 整批业务生成，避免历史 `submitted_unknown` 单元重复计费和覆盖已有资产。
- 历史 unknown billing 仍为 59，但没有活动中的未知计费；不得自动重投。
- 当前工作树保留用户既有的历史文档删除、旧日志修改和未跟踪研究文件，本轮没有清理、覆盖或提交它们。
- 用户曾在对话中直接粘贴 API Key；应在供应商后台轮换，交接文档、Git 和日志未保存该凭证。

## 7. 回家续接命令

```powershell
cd D:\VIDO
git status --short
git fetch --all --prune
git switch codex/story-ad-v3-upgrade
git pull --ff-only origin codex/story-ad-v3-upgrade
npm install
node src/server.js
```

本地访问：`http://localhost:3007`

`LAPTOP-LDFOL0GT` 只运行本次优化直接相关的定向测试，不默认执行 `platform:upgrade:test` 或跨版本完整回归。

## 8. 回家后的验收顺序

1. 新建广告与剧情各一个空白项目，确认必须显式选择内容类型，且切换类型会要求确认并使旧下游产物失效。
2. 粘贴多段长文本，刷新/重新进入，核对换行、段落、滚动和全文字符哈希不变。
3. 人物中心核对单人物主图、多套造型、鞋履/服饰/配饰原子卡、年龄状态和身份连续性；只做只读检查，不提交付费生成。
4. 场景世界核对地点预分配、机位、出场顺序、造型绑定、旧址重建关系和按单场景生成按钮。
5. 核对第 4 步剧本、第 5 步分镜/线稿、第 6 步场景音效与成片流程；广告和剧情的提示词、脚本及质量门禁不得串用。
