# VIDO 剧情广告引导式工作台 V106 公司续接交接

> 交接日期：2026-08-21（Asia/Shanghai）
> 当前权威分支：`codex/story-ad-systemic-remediation`
> 生产版本：`20260821-responsive-brief-v106`
> 用途：明天在公司电脑拉取后继续产品与交互优化。

## 1. 当日目标与用户决策

当日完成并上线的目标：

1. 立项改为引导式对话。用户给出完整内容时直接识别；缺少高价值信息时再追问；参考视频、链接与“无参考继续”均在对话内完成。
2. “手动编辑全部设置”降级为模态弹窗，移除正常文档流中的设置区和冗余“新增 / 修改内容”入口。
3. 用户确认设想后，系统以稳定幂等键自动生成剧情、动作与对白，再进入剧情页；双击、刷新和重挂载不得重复生成。
4. 业务错误严格按制作环节归属：具体错误只在所属环节显示，其他环节只允许导航徽标或紧凑提示。
5. 修复剧情列表列轨错位、操作按钮裁切和窄桌面变形。
6. 优化首屏阻塞读取，跳过未请求的大域投影与重复输出查询。
7. 修复立项页在不同桌面尺寸下输入区被截断或左侧出现大块空白的问题。

用户后续方向：继续参考专业视频工具，但不照搬无结构聊天框、万能提示词或第四套画布；优先考虑动作编排节拍、人物完整度面板、导演卡与复杂镜头可选预演。

## 2. 修改前后的完整数据流

### 2.1 立项与自动剧情

修改前：

`用户输入 → 文本原样追加 → 固定回复 → 右侧高级配置逐项选择 → 手动确认 → 进入剧情页后再次点击 AI 补全`

问题：对话、手动设置和高级配置形成三个主入口；参考链接不在对话内；确认后还需二次点击；历史参考草稿可能被误标为当前参考分析。

修改后：

`用户自然语言/参考材料 → 显式规格与世界设置识别 → 只追问缺失高价值项 → 同一项目确认单实时同步 → 对话内确认 → 以 content revision + 稳定 idempotency key 提交 blueprint → 持久化正式剧情 → 清除旧 reference_draft → 自动进入剧情与对白`

失败边界：提交失败不导航，按钮恢复；只有用户再次明确确认才重试；同一内容版本复用同一幂等键，不重复付费。

### 2.2 错误归属

修改前：

`任意业务阶段失败 → 公共 projectProgressHost → 所有页面显示同一失败正文`

修改后：

`业务阶段 → normalize stage → owning view 显式映射 → 所属页面显示具体错误；其他页面仅保留可发现的导航状态`

人物、场景、剧情、分镜、最终合成错误不会再跨环节覆盖当前工作。

### 2.3 首屏性能

修改前：

`等待 release 校验 → 全屏 loading → 读取完整 task bundle → 无条件投影全部资产/场景/视频域 → 重复查询 outputs → 渲染当前页`

修改后：

`先显示应用壳 → 当前页面只请求需要的 sections → summary/reference 不读取资产大域 → task summary 禁止重复 outputs/video boundary 查询 → 增量合并当前页数据`

真实大任务 summary/reference 服务端耗时由约 3.0–3.2 秒降至接近基础 task bundle 读取范围；仍应在公司继续采集 cold/warm API、parse、render 分段数据。

### 2.4 响应式立项布局

修改前：

`右侧确认单内容决定双栏总高度 + 左侧 align-self:start + 对话滚动区固定 max-height:390px`

结果：高屏左侧出现大块空白，低高度屏幕输入区被挤出或裁切。

修改后：

`动态视口高度 → 左侧 76px / minmax(0,1fr) / auto 三行网格 → 对话记录占剩余空间并独立滚动 → 输入区固定在工作台底部 → 1080px 窄桌面切单栏`

## 3. 代码与文件变更清单

主要生产文件：

- `public/story-ad/views/briefDialoguePanel.js`：引导式追问、参考上传/链接/无参考与确认入口。
- `public/story-ad/views/briefView.js`：单一表单权威、自动 blueprint 提交、成功导航和失败恢复。
- `public/story-ad/views/briefSettingsModal.js`：手动设置模态弹窗、焦点与关闭行为。
- `public/story-ad/components/ui.js`：生成错误的 owning view 隔离。
- `public/story-ad/views/plotBeatEditor.js`、`public/story-ad/workspace-ux.css`：剧情列轨与操作区修复。
- `public/story-ad/dialogue-theme.css`：动态视口高度、弹性对话区和窄桌面单栏。
- `src/services/storyAdWorkspace/projectBundleService.js`：轻量 sections 与按需投影。
- `src/services/storyAdWorkspace/referenceDraftProjectionService.js`：当前参考有效性与来源门禁。
- `scripts/run-with-pm2-env.js`：清除可能误导运行目录的 PM2 控制字段。

关键新增/更新回归：

- `scripts/test-story-ad-brief-modal-auto-blueprint-v103.js`：84 项，覆盖弹窗、自动剧情、幂等、失败恢复和六组桌面视口几何。
- `scripts/test-story-ad-dialogue-intake-v100.js`：完整输入识别与参考材料分支。
- `scripts/test-story-ad-lightweight-bundle-v100.js`：首包大域读取与重复 output 查询门禁。
- `scripts/test-story-ad-workspace-ux-v100-regressions.js`：错误归属、剧情表列轨与溢出。
- `scripts/test-story-ad-pm2-env-sanitization-v104.js`：旧运行目录环境字段隔离。

## 4. 提交记录、分支与公司电脑拉取命令

关键提交：

- `928dfaea`：引导式立项、错误归属、剧情布局首轮收口。
- `27e92bfd`、`72483e84`、`7fd2df2c`：轻量首包、去除重复查询和按需辅助模块。
- `e26d1c29`：设置弹窗、删除冗余入口、确认后自动剧情、旧参考隔离、PM2 环境清理。
- `203fcf22`：同步历史编辑发布契约。
- `708df268`：不同桌面高度的立项自适应修复。
- `e19afc1f`：V106 不可变发布构建。

本交接文件提交后，公司电脑使用实际权威分支，不使用协议模板中的旧 `codex/story-ad-v3-upgrade`：

```powershell
git status --short
git fetch --all --prune
git switch codex/story-ad-systemic-remediation
git pull --ff-only origin codex/story-ad-systemic-remediation
npm install
node scripts/test-story-ad-brief-modal-auto-blueprint-v103.js
node scripts/test-story-ad-dialogue-intake-v100.js
node scripts/test-story-ad-lightweight-bundle-v100.js
$env:PORT='3007'
node src/server.js
```

禁止在公司电脑有未提交修改时使用 `git reset --hard`。5 份家庭电脑既有未跟踪历史文档未纳入本次交接，也不属于生产制品。

## 5. 本地、Git、生产三方一致性

| 核对对象 | 结果 | 证据 |
|---|---|---|
| 本地源码 | 一致 | 构建提交 `e19afc1f`；受跟踪文件干净；运行清单 source revision `708df26830498ff87166ce1a71c127c03dad6be0` |
| origin/Gitee | 一致 | `codex/story-ad-systemic-remediation` 与本地 ahead/behind `0/0` |
| GitHub 镜像 | 一致 | 原落后 235 个提交，本轮已安全快进到与本地相同提交 |
| 生产运行代码 | 一致 | `/opt/vido/current` 指向 artifact `a1845f237581e395a981c18f645f056b06071135fb6e12b40232365922a04b3f`；794 个 runtime manifest 条目逐项 SHA-256 复算，mismatch `0` |
| 发布身份 | 一致 | build `20260821-responsive-brief-v106`；bundle `a920ce166158b1195df414d783555cc6471e4e94ba3703947d3914482fedff5a`；runtime hash `18b732dd5e91437fbe8dabdab7feaa5a2b3b888a5fe3be961453dcf7093eea6a` |

说明：交接 MD 提交后，本地/Git HEAD 会比生产 source revision 多一个纯文档提交；生产代码仍按不可变清单与 `708df268` 源码树一致，不构成代码差异。

## 6. 实际执行的验证

### 静态与定向

- `node --check`：相关 JS 通过。
- 引导立项：14 项通过，模型调用 0。
- 弹窗、自动剧情与响应式：84 项通过；stub blueprint 调用 3，真实模型调用 0。
- 轻量 Bundle：10 项通过，模型调用 0。
- 工作台边界：通过；initial JS 79,650 bytes，core gzip 104,501 bytes。
- 工作台错误归属与剧情几何：64 项通过，模型调用 0。

### V106 完整发布门禁

- `systemic`：通过，21.245 秒。
- `workspace_ui`：通过，9.633 秒。
- `narrative_v111`：通过，304.442 秒；10,000 固定种子、400 组变形、50 并发任务、重复许可 0、付费 provider 调用 0。
- `release_core`：通过，81.295 秒；发布完整性 11 项、不可变传输 29 项、发布闭包 795 项。

### 生产只读核对

- PM2 `vido`：online，restart 0，PID 13459；exec cwd 与 script path 都指向 V106 不可变目录。
- 内网 `127.0.0.1:4600`、公网 `https://vido.smsend.cn`、本地 `localhost:3007`：health 均为 ok，版本和 runtime hash 一致。
- SQLite：`PRAGMA quick_check` 为 `ok`，数据库路径 `/data/vido/db/vido.sqlite`。
- 生产只读系统审计：31 个任务、31 个 authoritative Work、active generation 0、active unknown billing 0、orphan output task 0。
- 历史隔离 unknown billing 60 保持，未作为活动任务自动重试。
- 生产 runtime manifest 自身 SHA-256：`0e09db46f76b10b35d877435bb7edfb98d1a66f700b962ab1b9a56f5f813db33`，与本地相同。

## 7. 未执行项、剩余风险、费用与数据边界

未执行项：

- 家庭电脑按用户规则未运行 `platform:upgrade:test` 等全平台跨版本回归；本次直接相关的 full 发布门禁已全部执行。
- 未执行真实文字模型、图片或视频生成；没有用真实生成来验证交接。
- 未替用户确认参考理解、人物资产、费用或生成许可。

剩余限制与风险：

1. 当前工作台全局最低支持宽度仍为 1080px；手机/平板需要单独重构顶栏、侧栏与导航，不应只靠局部 CSS。
2. 轻量首包已显著减少无关读取，但大型真实项目仍应继续记录 cold/warm TTFB、JSON parse 和 render 三段耗时。
3. 生产保留 60 条历史、已隔离的未知计费记录；活动未知为 0。不得把历史记录自动重试或删除。
4. 5 份家庭电脑既有未跟踪文档未纳入 Git；公司电脑不会拉到这些文件，也不影响受控代码一致性。

费用与覆盖边界：本轮三方核对和交接生成均为只读；模型、图片、视频调用 0；没有覆盖用户项目数据、历史日志或未跟踪文件。

## 8. 明天继续优化的入口与顺序

建议按以下顺序继续：

1. 先按第 4 节命令拉取，确认 `git rev-list --left-right --count "HEAD...@{upstream}"` 为 `0 0`，运行三条立项/性能定向回归。
2. 用真实项目在 1920×1080、1600×900、1440×760、1366×680、1280×640、1080×720 复核立项页；重点观察浏览器缩放 80%/100%/125% 和系统显示缩放。
3. 性能：建立导航、summary API TTFB、payload、parse、render 和 long task 记录；先测后改，不再用整页 loading 掩盖。
4. 产品 P1：设计“动作编排节拍”结构，至少包含参与者/武器、空间与轴线、攻击、闪避/防御、反击、受击反馈、镜头和连续性；先做 schema 与黄金回归，不直接写万能 prompt。
5. 产品 P1：在现有人物档案上补“完整度面板”，展示基础设定、三视图、造型、情绪、习惯动作、一致性 QA 及受影响镜头，不新建第二套人物资产系统。
6. 产品 P2：把现有结构化导演参数汇总成可解释的导演卡；复杂人物走位与运镜再评估可选 3D 预演/控制视频链路，简单镜头不强制使用。

生产只读复核命令：

```powershell
ssh -o BatchMode=yes vido-prod
```

新公司电脑如果尚未配置独立 SSH 公钥，先按 `docs/handoffs/HANDOFF_PROTOCOL.md` 初始化；禁止通过 Git、交接 MD 或聊天复制私钥和服务器凭证。
+

## 9. 2026-08-21 交互与全平台响应式补充（V109）

### 本轮已完成

- 新建项目不再预置助手对白或快捷回答，页面初始为空，由用户先发起对话。
- 用户消息经 `brief_dialogue` 专用服务结合内容类型、累计创意、当前流程、参考材料状态和最近对话生成上下文应答；前端以逐字方式呈现。模型异常时只使用当前输入生成兜底回复，不复用旧项目内容。
- 长文本框支持右下角纵向拖动，并提供“展开输入/收起输入”按钮；展开后对话区内部重新分配高度，不制造页面空白滚动。
- 原硬编码 `20% / 38% / 78% / 100%` 已删除。当前显示“立项准备度”，按实际确认信息计算：内容类型 20%、核心内容 40%、项目名称 10%、参考决定 20%、最终确认 10%。新项目初始值为 0%。
- 问询不是“高级设置问卷”。剧情内容按缺项询问人物、事件、观众感受；商业内容按缺项询问产品/服务、核心价值、目标受众和行动目标。信息充分时不重复追问，最多补 1–2 个关键主题；项目名、规格和世界观等高级项可由系统建议或用户在高级设置中手动调整。
- 响应式范围扩大到整个剧情广告平台：任务中心、六个制作步骤、工作流画布、弹窗和抽屉。最低桌面基线覆盖 1080 宽；真实长内容允许纵向滚动，宽表由自身容器横向滚动，禁止整页横向溢出、控件截断和无内容空白滚动。
- 任务中心在 1280px 及以下切换为两行卡片式项目排布；1080px 且存在浏览器纵向滚动条时的 15px 全局横向溢出已从 `body` 根规则修复。

### V109 发布身份与三方现状

| 核对对象 | 结论 | 证据 |
|---|---|---|
| 本地源码与发布包 | 一致 | build `20260821-dialogue-interaction-v109`；artifact `ca4b807a8c30d2daece9b2cf68ab287333de10c3a2451738800cb59200299d8c`；797 个 runtime 文件；source revision `cbc68819952aa7e8c5f44e5f072b30d1c790f39a` |
| origin/Gitee | 一致 | 当前发布提交 `f86480e62f62775325a262c9521915fd5f4694b6` 已推送，ahead/behind `0/0` |
| 生产服务器 | **尚未同步 V109** | 生产仍为 V106：build `20260821-responsive-brief-v106`，artifact `a1845f237581e395a981c18f645f056b06071135fb6e12b40232365922a04b3f`，source revision `708df26830498ff87166ce1a71c127c03dad6be0` |
| 交接 MD 两份副本 | 本节写入后重新校验 | 仓库 `docs/handoffs/` 与 `C:\Users\User\Videos\` 同名文件必须保持 SHA-256 一致 |

生产没有在本轮被改动。PM2 `vido` 仍为 online、restart 0，运行目录仍指向 V106 不可变 artifact；内网 health 为 `ok`。因此当前准确结论是“本地与 Git 一致，生产健康但版本落后”，不能表述为三方代码一致。

### 本轮实际验证

- 对话服务与验证器：20 项通过，真实模型调用 0。
- 立项状态与进度计算：17 项通过，模型调用 0。
- 弹窗、自动剧情、用户发起和逐字交互：84 项通过；stub blueprint 调用 3，真实模型调用 0。
- 平台响应式门禁：36 项通过；覆盖 1920×1080、1600×900、1440×760、1366×680、1280×640、1080×720。
- V109 真实本地任务中心：上述 6 组视口横向溢出均为 0，关键按钮越界数均为 0；1280px 及以下正确切换卡片布局。
- V109 真实本地立项页：上述 6 组视口横向及外层空白纵向溢出均为 0；初始助手消息 0、进度 0%、输入框 `resize: vertical`；展开高度由 57px 增至约 173px。
- 完整 `story-ad:release:test` 通过；发布闭包 798 个文件，测试中的模型/媒体调用均为 0。
- 本地 `/api/health` 为 `ok`；生产 `127.0.0.1:4600/api/health` 为 `ok`。
- 未执行真实文字模型、图片或视频生成；未产生生成费用。
- 未执行项：V109 生产部署与部署后 V109 清单/公网页面复核，因为本轮没有获得明确生产发布授权。

## 10. 2026-08-21 最终生产交接（V114，覆盖前述 V109 状态）

本节是本文件当前权威结论，覆盖第 9 节“生产尚未同步 V109”的历史状态。V114 已完成发布并切换生产流量。

### 发布身份与三方结论

| 核对对象 | 当前身份 | 结论 |
|---|---|---|
| 本地运行态 | build `20260821-dialogue-interaction-v114`；runtime hash `72e07c9d08d9ae5159f7eb456d2be83d1ce0fde8ac70d041cf258dbfb4d39be8` | health `ok`，旧版剧情广告 UI 开关为 false |
| Git / origin | 分支 `codex/story-ad-systemic-remediation`；V114 构建提交 `0397d48ed2c8b4234f958f50a4f133c32d72618a` | 本地与 origin ahead/behind `0/0`；本节交接文档提交后再次同步 |
| 生产运行态 | artifact `2054c343b06829040a28d4254a9f7d86a8bbdcc76154a21a75b07cbdf11d3011`；source revision `0e0b021b33c669f61bed7ed3e5aa688b08a41160`；bundle `e8ad2b999eb271176189d03e22482bed007f2e0f7fb3e807ef684ac8e657537e` | PM2 `vido` online，restart 0，生产 health `ok`，runtime hash 与本地一致 |

生产 `/opt/vido/current` 已指向 V114 不可变发布目录。V113/V106 仅保留为回滚目录，不承接当前流量。旧 `public/story-ad/dialogue-demo.{html,js,css}` 不在 V114 发布目录，`legacy_story_ad_ui_enabled=false`，因此不存在新旧两套交互同时运行。

### V114 修复与验证

- 在 900px 及以下将平台侧栏改为横向步骤导航、主区改为单列；压缩顶栏动作和任务中心布局。520px 及以下进一步收紧品牌区、统计卡和表格行，修复真实生产 390px 视口约 75px 根级横向溢出的最早来源。
- 平台响应式定向回归 54 项通过，覆盖 1920×1080、1600×900、1440×760、1366×680、1280×640、1080×720、900px、768px、390px 共 9 组视口。
- 生产真实浏览器复核 1920×1080、1536×864、1366×768、1280×720、768×1024、390×844，根级横向溢出均为 0；桌面页面高度等于视口，无空内容滚动；移动端纵向滚动仅承载真实堆叠内容。
- 390×844 生产页面输入框初始约 57px、展开约 190px、收起恢复约 57px，支持纵向拖动；初始对话为空，立项准备度为 0%。
- 立项准备度权重为：内容类型 20%、核心内容 40%、项目名称 10%、参考决定 20%、最终确认 10%。它不是高级设置问卷进度。
- 问询依据用户当前输入和缺失信息动态生成：剧情侧关注人物、事件和观众感受；商业侧关注产品/服务、核心价值、目标受众和行动目标；信息充分后不重复追问。项目名、规格和世界观等仍可由系统建议或在高级设置中调整。
- 发布门禁完成 10,000 seed、400 组变形与 50 并发测试，重复结果 0、付费 provider 调用 0；发布闭包 798 个文件。SQLite `PRAGMA quick_check=ok`，活动生成 0、活动未知计费 0，历史隔离未知计费 60 保持且未自动重试。

### 未执行项与剩余边界

- 未在生产点击“发送”触发真实文字模型，也未执行图片或视频生成，避免产生费用；逐字流式与上下文问答由定向回归验证，生产页面只做无费用交互和布局验收。
- 生产直接访问部分未解锁步骤会显示加载/锁定态；自动化响应式门禁已覆盖任务中心、六个制作步骤、工作流画布、弹窗和抽屉。
- 已知未修复阻塞项：无。模型、图片、视频调用：0；用户项目数据覆盖：0。

## 11. 2026-08-21 底部空白修复（V115，当前生产）

- 用户反馈的输入区下方空白已追踪到 `.brief-dialogue-view` 固定的 26px 底部装饰 padding；V115 将其调整为 0，并取消贴合视口底边处的底部圆角。
- 当前生产 build `20260821-dialogue-layout-v115`，artifact `9b6ca353b317b38957d40f736a1e79d2ccff9ca3b41322ed36c8c9e9937c8351`，source revision `5c737a6d09d0740e38e123dd74639fe74bcd08b8`，runtime hash `5da1f6942cac930d9c6b442890dfb7320ecd4ec663bb5d5e09db142320843c18e`。
- 生产 1435×768 真实浏览器验收：面板底部间距 0、横向溢出 0、页面 `scrollHeight=clientHeight=768`；初始消息 0。
- 对话/立项/弹窗定向回归 123 项、平台响应式 54 项通过；完整发布门禁通过，798 个运行文件校验一致，付费 provider 调用 0。
- PM2 `vido` online、restart 0；内外网 health `ok`；活动生成 0、活动未知计费 0。V114 仅保留回滚，不承接流量。

## 12. 2026-08-21 立项问询闭环（V116，当前生产）

本节覆盖第 11 节的“当前生产”状态。V116 已切换生产流量，立项流程固定为“核心想法 → 成片规格 → 参考材料 → 整体确认”，不能跳过成片规格或参考材料决定。

- 根因：旧状态机只有 `idea → reference → review`；120 秒、9:16、1080p 虽显示在确认单中，却没有“用户已确认”状态；在参考阶段输入“没有”仍被追加到核心创意并调用完整文字模型，造成不必要的约 10 秒等待。
- 修复后：成片规格增加独立确认阶段，显示系统建议的时长、画幅、清晰度并标记“建议·待确认”；用户可“确认当前规格”或进入高级设置调整。三项确认后才进入参考材料问询，参考材料决定后才允许整体确认。
- 参考问题会结合内容类型和核心创意生成：剧情内容询问人物形象、时代氛围、影视画面和镜头参考；商业内容询问产品素材、品牌视觉、竞品视频和节奏参考。输入“没有”在参考阶段由本地状态机即时处理，不调用模型，也不写入核心创意。
- 立项准备度调整为：内容类型 15%、核心内容 35%、项目名称 10%、成片规格 20%、参考材料决定 10%、整体确认 10%。它表示必要信息的实际确认状态，不是高级设置填写进度。
- 当前生产 build `20260821-dialogue-intake-v116`，artifact `e502c98ccd4cc548a4a61bf48142f7045365ae66202b33780f52204134c2b7f4`，source revision `4c47dac3f411b3c6fe78f7ba6f6634b94f9d6917`，runtime hash `7e8b95f66f3a7db97e3e154045e3d5508ae6304c52ed6a21a684ca3a1612a217`。
- 本地与生产版本、artifact、source revision、runtime hash 完全一致；Git HEAD/上游为构建提交 `c099a9082b45794af4df923eb52739f526fba13c`，ahead/behind `0/0`。
- PM2 `vido` online、restart 0，运行目录 `/opt/vido/releases/e502c98ccd4cc548a4a61bf48142f7045365ae66202b33780f52204134c2b7f4`；内外网 health 均为 `ok`，旧版 UI 开关为 false。
- 定向回归：对话服务 22 项、立项状态 27 项、弹窗/蓝图 88 项、9 组视口响应式 54 项通过；真实模型/图片/视频调用均为 0。浏览器集成验证中，规格确认前参考问题不可见且整体确认禁用；确认规格并输入“没有”后，模型调用计数保持 1、“没有”未进入核心创意、进度为 90%、整体确认才启用。
- 完整生产发布门禁通过：10,000 seed、400 组变形、50 并发，重复许可 0、付费 provider 调用 0；799 个生产文件逐项校验一致。SQLite `PRAGMA quick_check=ok`，活动生成 0、活动未知计费 0，历史隔离未知计费 60 未重试。

未执行项：未在生产发送真实用户内容触发文字模型，未执行图片或视频生成，避免付费和覆盖用户数据。当前没有已知阻塞项。

## 13. 2026-08-21 专业立项问询与确认证据修复（V117，当前生产）

本节覆盖第 12 节的“当前生产”状态。V117 已切换生产流量。

- 根因一：旧代码用 `!requireUserInitiation` 初始化规格确认状态，导致已有项目只因不是新建路由就显示“用户已确认”，并非来自用户对话证据。
- 根因二：旧导演助理规则把时代、地区、视觉形态和制作方式定义为可选精调，只用人物、事件、情绪三个条件即可放行，因此会跳过世界时空、视觉方向、目标受众和完整叙事链。
- V117 取消路由推断确认。创作方向、规格确认和参考决定均写入 `brief_intake` 并持久化；旧项目没有明确证据时一律回到待确认。只修改时长、画幅、清晰度中的一项不会确认整组规格。
- 专业立项必须覆盖五类直接依据：人物/主体、叙事或价值演示链、受众与表达目标、世界时空、视觉制作方向。服务端要求每类已确认项附带用户累计内容中的原文证据；证据不存在、模型编造证据，或仅有“古代”“现代”“电影感”等宽泛词时，强制退回继续追问。
- 问询每轮只补 1–2 个最影响制作的缺口，并结合当前故事提供可选方向；禁止“针对【原始创作需求】”“核心已经成立”等模板话术。五类未完整时不能进入成片规格，规格未明确确认时不能进入参考材料，参考决定未明确时不能生成剧情与对白。
- 立项字号收紧：导演标题 15px、确认单标题 19px、对话与输入 12px、快捷按钮 11px、发送按钮 12px；九组响应式视口仍保持无根级横向溢出和无空内容纵向滚动。
- 当前生产 build `20260821-professional-intake-v117`，artifact `b316674b3f735534263029486a09053b1ca3ff0d6b326d0419ac0e99073841ae`，source revision `f2a54625cdb6960d98de3a808c91a208b2878da1`，runtime hash `79073bef534867d892d8e08aaad0073f259a6a9c385f7331e3767d072dd5b00c`。
- 定向回归：导演助理 28 项、立项状态 30 项、弹窗/蓝图 88 项、响应式 54 项通过；V6/V3 边界通过。发布门禁完成 10,000 seed、400 组变形、50 并发，重复许可 0、付费 provider 0；799 个生产文件逐项校验一致。
- 生产 PM2 `vido` online、restart 0；内外网 health `ok`，SQLite `quick_check=ok`，活动生成 0、活动未知计费 0，历史隔离未知计费 60 未重试。

未执行项：没有在生产发送用户故事触发真实文字模型，也没有生成图片或视频，避免费用和创建测试项目。生产页面以只读方式验收 V117 脚本、字号、初始 0%、三项“建议·待确认”、确认按钮禁用以及页面无空白滚动。
