# VIDO 周末开发交接（2026-07-09 晚—2026-07-12）

## 本次交付范围

- C 端创作工作台：首页改为用户作品、未完成任务快速续作、失败任务处理、分页和管理员全量视图；支持亮/暗主题及统一导航。
- 视频画布：移除冲突的工作流画布入口，统一平台视觉与返回工作台位置，保留画布能力并接入流式应答交互。
- 新剧情广告：补齐场景空间契约、多视图一致性、人物与场景分离更新、镜头连续性、剧本中文化和精品质量门禁。
- 生成可靠性：异步任务、刷新恢复、取消生成、超时、失败快速收口、网络断开回查、逐张关键帧真实进度。
- 数据治理：同任务保存不再生成重复展示记录，存量重复任务完成清理并持续去重。
- 性能与查看：场景缩略图、本地资源回源、首帧/缩略图展示、分页加载和状态懒恢复。

## 新剧情广告关键修复

- 已确认剧本的 beat 数作为分镜硬约束；缺镜时仅对缺失 beat 定向补生成，仍不一致则失败。
- 正常业务词“AI 模型、模型库、模型选择”不再被误判为内部流程；真实后台、Prompt、QA 和模型失败描述仍阻断。
- 写请求遇到 `Failed to fetch` 或连接重置后自动回查服务器，已接收则恢复轮询，避免重复提交。
- 新生成以生成 ID 和阶段绑定计时，上一轮 `generation_started_at` 不再覆盖当前进度。
- 各阶段支持真实取消，取消后停止后续供应商候选并拒绝保存迟到结果。

## 生产数据与验证

- 生产剧情广告重复任务由 16 条清理到 7 条；4 组重复、9 条旧记录已在逻辑备份后删除，重复组为 0。
- 受影响分镜任务真实重跑成功：6/6 镜、QA 硬阻断 0、进入 `keyframe_contract_ready`。
- 场景四视图、基础信息恢复、中文输出、精品剧本、进度、取消、可靠性和商业准备度专项测试均已通过。

## 明日继续处理

- 漫路 `gpt-image-2` 当前固定调用 `/ent/v1` 企业图片接口，消耗国内人民币通道；人民币余额为负时返回 `1005 Insufficient quota`，美元余额不能支付该接口。
- 当前账务熔断粒度为供应商级，国内图片通道额度不足会阻止同供应商海外图片候选。建议改为“供应商 + 计费通道”级熔断，并对真实可用的美元图片模型做单张生产验证。
- 生产当前使用漫路主账号下名称为“最新”的启用密钥；同主账号换另一条密钥不会改变钱包额度。

## 安全说明

- 仓库不包含服务器密码、平台登录密码、API Key、Token、生产数据库、运行日志、下载视频或临时调试产物。
- 部署与运维脚本均从环境变量读取凭证。
