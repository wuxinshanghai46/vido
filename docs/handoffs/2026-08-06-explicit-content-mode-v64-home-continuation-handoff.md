# VIDO v64 回家续接交接

> 日期：2026-08-06
> 分支：`codex/story-ad-v3-upgrade`
> 生产：`43.98.167.151` · `/opt/vido/app` · PM2 `vido` · 端口 `4600`

## 1. 本轮目标与用户决策

- 在创建剧情广告项目之前，由用户显式选择“广告”或“剧情”；未选择时禁止 AI 帮写、创建项目和创建人物/场景方案。
- 用户选择是最高权威来源，不再让旧数据、参考识别或关键词猜测覆盖。
- 当前验收案例应是纯剧情：现代与古代两条时空线交替、共 2 位女孩、核心地点为竹海、商品数为 0。
- 本次交接前必须确认本地、Git、生产三方运行代码一致，并保证回家全新拉取可以复现生产字节。

## 2. 关键数据流：修改前与修改后

### 内容模式

- 修改前：用户原始文字 → AI/旧数据推断广告目标 → 可能把纯故事商品化，并覆盖人数与场景权威。
- 修改后：用户显式选择广告/剧情 → 写入 `content_mode_source=user` → AI 帮写、人物/场景规划、后续脚本统一消费该权威字段；剧情模式允许商品为空。

### 发布可复现性

- 修改前：本地与生产 v63 的运行文件相同，但 146 个文本文件含混合/CRLF；Git 按 `.gitattributes` 检出 LF，导致回家新克隆与生产字节不一致。
- 修改后：运行清单内文本统一为 LF，业务语义 diff 为 0；重新构建 v64。Git HEAD、本地、生产均按同一清单逐文件校验。

## 3. 主要代码与发布内容

- 广告/剧情显式选择、权威持久化、未选择门禁和高对比交互已在 v63 完成。
- v64 版本：`20260806-reproducible-handoff-v64`。
- 合同版本：`reference-director-v4`。
- 运行哈希：`0dfe7db0fa087cae45809df493d0b4d2ed4341cb913867af91228b2ef9e6d90d`。
- 发布闭包：297 个运行文件；加发布清单共 298 个发布文件。
- v64 只解决检出/发布字节可复现性并刷新前端缓存版本，没有改变业务逻辑。

## 4. Git 记录与回家拉取

- 功能提交：`68525d4`（显式广告/剧情模式）。
- v63 部署记录提交：`ac7ede7`。
- v64 可复现发布提交：`4c1272d`。
- 当前唯一配置远端：`origin`（Gitee）。

回家后在已有仓库中执行：

```powershell
git status --short
git fetch --all --prune
git switch codex/story-ad-v3-upgrade
git pull --ff-only origin codex/story-ad-v3-upgrade
npm install
npm run platform:upgrade:test
$env:PORT='3007'
node src/server.js
```

访问：<http://localhost:3007>

如果回家电脑有未提交文件，先保留并检查，不要使用 `git reset --hard` 或覆盖本地工作。

## 5. 三方一致性结论

| 核对对象 | 版本/提交 | 逐文件结果 | 运行状态 |
|---|---|---:|---|
| 本地 | v64 / `4c1272d` | 297/297，差异 0 | 本地 3007 返回 v64 |
| Git Origin | `4c1272d` | 从 HEAD blob 读取 297/297，差异 0 | ahead/behind 0/0 |
| 生产 | v64 / 运行哈希见上 | 297/297，差异 0；298/298 发布文件一致 | PM2 online，内外网健康 ok |

结论：三方运行代码一致；回家从当前分支拉取可复现生产运行文件。

## 6. 已执行验证

- `npm run story-ad:release:build`：通过，37 个静态文件、297 个运行文件。
- `npm run story-ad:release:test`、`npm run story-ad:v6:boundaries`：通过。
- `npm run story-ad:v6:test` + `npm run story-ad:v3:test`：退出码 0，约 303.8 秒；真实付费模型调用 0。
- 生产部署前完整回归与原子发布：退出码 0，约 489.3 秒。
- Git HEAD blob 审计：297/297，差异 0。
- 本地独立 SHA-256：297/297，差异 0。
- 生产独立 SHA-256：297/297，差异 0。
- 生产切换前/后活动生成任务：0/0。
- PM2 `vido`：online；内网/公网健康：ok；数据库：ok；SQLite `quick_check`：ok。
- 本轮没有触发真实图片、视频或业务模型调用，也没有改写旧任务数据。

## 7. 未执行项、限制与风险

- 未执行付费出图/视频验收：按用户要求留到回家后的完整人工验收，避免重复计费。
- 当前旧目标任务仍包含历史错误内容；v64 不会自动替用户改写。必须手动选择“剧情”并修正内容目标后再创建新方案。
- 图片供应商此前的不透明 5xx 属于外部系统风险，平台已保留成功 checkpoint、停止盲目自动重试并显示详细错误，但不能保证外部服务永不失败。
- 本机只配置了 Gitee `origin`，没有可独立核对的 GitHub remote。
- 工作树仍有用户原有的旧日志、删除标记和未跟踪研究文档；它们不属于运行清单，也未纳入本轮提交。

## 8. 回家后的建议验收顺序

1. 拉取分支并确认 `/api/story-ad/version` 返回 v64。
2. 打开原任务，在内容模式中明确选择“剧情”。
3. 内容目标填写：`现代与古代两条时空线交替叙事，各有一位女孩，共2人；核心场景为竹海，女孩在竹海漫步；这是纯故事剧情，不含商品、品牌和购买转化。`
4. 保持商品/产品主体为空，再创建人物与场景方案。
5. 在任何付费生成前核对：人物 2、竹海场景存在、书房未擅自替换、商品 0。
6. 方案正确后再进行人物、场景、分镜和视频的完整验收；若方案仍错误，先停在规划阶段，不要触发付费生成。
